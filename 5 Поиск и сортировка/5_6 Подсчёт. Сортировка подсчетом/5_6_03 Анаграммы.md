Анаграммы

Слово называется анаграммой другого слова, если оно может быть получено перестановкой его символов.

Входные данные
Даны два слова на отдельных строках. Слова состоят из строчных латинских букв и цифр. Длины слов не превышают 255.

Выходные данные
Требуется вывести "YES"  – если введенные слова являются анаграммами друг друга, "NO"  – если нет.

Sample Input:
sharm
marsh

Sample Output:
YES

Напишите программу. Тестируется через stdin → stdout
Верно решил 131 учащийся
Из всех попыток 61% верных


# ruby
def sortingByCount(m)
    k = m.max 
    scope = k + 1
    c = [0] * scope
    
    0.upto(m.size-1) do |i|
        c[m[i]] += 1
    end  
  
    m = []    
    0.upto(k) do |j|
        0.upto(c[j]-1) do |i|
            m.push(j)
        end
    end

    return m     
end

s1 = gets.chomp.split('').map{|j| j.to_s.ord}
s2 = gets.chomp.split('').map{|j| j.to_s.ord}

if sortingByCount(s1) == sortingByCount(s2)
    print "YES"
else
    print "NO"    
end


# python
d = {}

for c in input(): d[c] = d.get(c, 0) + 1
for c in input(): d[c] = d.get(c, 0) - 1

print(('YES', 'NO')[any(d.values())])


# JS
var stdin = process.openStdin();

stdin.addListener("data", function(d) {
    let [word1, word2] = d.toString().split('\n');    
    let result = (word1.length === word2.length) ? 'YES' : 'NO';
    word2 = word2.split('');

    for (let i=0; i<word1.length; i++) {
        let a = word2.indexOf(word1[i], 0);
        if (a===-1) {
            result = 'NO';
            break;
        } else word2[a]=-1;   
    }
     console.log(result);
  });


# C#
using System;

byte[] charsCount;

{
    string first = Console.ReadLine();
    string second = Console.ReadLine();

    if (first.Length != second.Length) { Console.WriteLine("NO"); return; }

    charsCount = new byte['z' + 1];
    for (int i = 0; i < first.Length; i++)
    {
        charsCount[first[i]]++;
        charsCount[second[i]]--;
    }
}

string anagrams = "YES";
foreach (var count in charsCount)
    if (count != 0) { anagrams = "NO"; break; }

Console.WriteLine(anagrams);


# java
import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
        Scanner input = new Scanner(System.in);
        char[] theFirst = input.nextLine().toCharArray();
        char[] theSecond = input.nextLine().toCharArray();

        theFirst = countingSort(theFirst);
        theSecond = countingSort(theSecond);

        System.out.println(new String(theFirst).equals(new String(theSecond)) ? "YES" : "NO");
    }

    private static char[] countingSort(char[] array) {
        int max = array[0], counter = 0;

        for (int a = 1; a < array.length; a++) {
            if(array[a] > max) max = array[a];
        }

        int[] temporary = new int[max + 1];

        for (int b = 0; b < array.length; b++) {
            temporary[array[b]] += 1;
        }

        for (int e = 0; e < temporary.length; e++) {
            if(temporary[e] > 0) {
                for (int f = 0; f < temporary[e]; f++) {
                    array[counter] = (char)e;
                    counter++;
                }
            }
        }

        return array;
    }
}

