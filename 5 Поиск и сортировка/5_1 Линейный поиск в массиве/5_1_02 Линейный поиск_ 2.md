Линейный поиск - 2

Напишите программу, которая определяет, встречается ли заданное число x в данном массиве.

Входные данные
В первой строке задается одно натуральное число N, не превосходящее 1000 – размер массива.
Во второй строке вводятся N чисел – элементы массива (целые числа, не превосходящие по модулю 1000).
В третьей строке содержится одно целое число x, не превосходящее по модулю 1000.

Выходные данные
Вывести YES , если число x встречается в данном массиве, и NO в противном случае.

Sample Input:
5
1 2 3 4 5
3

Sample Output:
YES

Напишите программу. Тестируется через stdin → stdout
Верно решили 573 учащихся
Из всех попыток 74% верных


# ruby
n = gets.to_i
a = gets.chomp.split(' ').map { |j| j.to_i }
x = gets.to_i
print a.include?(x) ? 'YES' : 'NO'


# python
_, *lst, x = open(0).read().split()
print('YES' if x in lst else 'NO')


# JS
var stdin = process.openStdin();

stdin.addListener("data", function(d) {
    let str = d.toString().split('\n');
    console.log(str[1].split(' ').includes(str[2]) ? 'YES' : 'NO');
  });


# C#
using System;
using System.Linq;

public class MainClass
{
    public static void Main()
    {
        string non=Console.ReadLine();
        string[] str=Console.ReadLine().Split();
        string s=Console.ReadLine();
        Console.WriteLine(str.Contains(s)?"YES":"NO");
    }
}


# java
import java.util.Arrays;
import java.util.Scanner;
import java.util.stream.IntStream;

class Main {
    public static void main(String[] args) {
        try (Scanner scanner = new Scanner(System.in)) {
            int n = Integer.parseInt(scanner.nextLine());
            int[] ints = IntStream.generate(scanner::nextInt)
                                  .limit(n)
                                  .toArray();
            int x = scanner.nextInt();
            System.out.println(Arrays.stream(ints).anyMatch(value -> value == x) ? "YES" : "NO");
        }
    }
}


# C
#include <inttypes.h>
#include <stdio.h>
#include <stdlib.h>

void input(size_t* N, int64_t** arr, int64_t* X);
void echo_array(int64_t** arr, size_t N);
size_t count_x(int64_t** arr, size_t N, int64_t X);

int main() {
    size_t N = 0;
    int64_t* array = NULL;
    int64_t X = 0;
    input(&N, &array, &X);
    if (N) {
        printf("%s", count_x(&array, N, X) ? "YES" : "NO");
        free(array);
    }
    return 0;
}

size_t count_x(int64_t** arr, size_t N, int64_t X) {
    size_t count = 0;
    for (size_t i = 0; i < N; i++)
        if (X == (*arr)[i]) count++;
    return count;
}

void echo_array(int64_t** arr, size_t N) {
    for (size_t i = 0; i < N; i++)
        printf("%"PRId64"%s", (*arr)[i], i + 1 == N ? "" : " ");
}

void input(size_t* N, int64_t** arr, int64_t* X) {
    int64_t innum;
    *N = (scanf("%"SCNd64, &innum) == 1 && innum > 0) ? innum : 0;
    if (*N > 0) *arr = malloc(*N * sizeof(int64_t*));
    if (*arr == NULL) *N = 0;
    if (*N > 0)
        for (size_t i = 0; i < *N; i++)
            scanf("%"SCNd64, &((*arr)[i]));
    if (*N > 0) scanf("%"SCNd64, &(*X));
}


# C++
#include <iostream>

using namespace std;

int main() {
    int n;
    cin >> n;
    int arr[n];
    
    for (int i = 0; i < n; i++){
        cin >> arr[i];
    }
    
    int enter;
    cin >> enter;
    int count = 0;
    
    for (int i = 0; i < n; i++) {
        if (enter == arr[i])
            count++;
    }
    
    cout << (count == 0 ? "NO" : "YES");
    return 0;
}
