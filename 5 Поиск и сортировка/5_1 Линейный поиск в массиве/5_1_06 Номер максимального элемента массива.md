Номер максимального элемента массива

Напишите программу, которая находит номер максимального элемента массива.

Входные данные
В первой строке задается одно натуральное число N, не превосходящее 1000 – размер массива.
Во второй строке вводится N чисел – элементы массива (целые числа, не превосходящие по модулю 1000).

Выходные данные
Вывести одно число – номер максимального элемента в массиве (номера элементов в массиве считаем начиная с единицы). 

Sample Input:
5
5 4 3 2 1

Sample Output:
1

Напишите программу. Тестируется через stdin → stdout
Верно решили 448 учащихся
Из всех попыток 69% верных


# ruby
n = gets.to_i
a = gets.chomp.split(' ').map { |j| j.to_i }
p a.index(a.max)+1


# python
n, m = input(), [int(i) for i in input().split()]
print(m.index(max(m)) + 1)


# JS
var stdin = process.openStdin();

stdin.addListener("data", function(d) {
    let str = d.toString().split('\n');
    str = str[1].split(' ');
    console.log(str.indexOf(Math.max(...str).toString())+1);
  });

  
# C#
using System;
using System.Linq;

public class MainClass
{
    public static void Main()
    {
        string non=Console.ReadLine();
        int[] arr=Array. ConvertAll(Console.ReadLine().Split(), int.Parse);
        int max=arr.Max();
                                Console.WriteLine(Array.FindIndex(arr, i=>i==max)+1);
    }
}



# java
import java.util.Scanner;

class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int N = sc.nextInt();
        int max = sc.nextInt();
        int index = 0;
        for(int i = 1; i < N; i++){
            int a = sc.nextInt();
            if(a > max){
                max = a;
                index = i;
            }
        }
        System.out.println(++index);
    }
}


# C
#include <inttypes.h>
#include <stdio.h>
#include <stdlib.h>
#include <limits.h>

void input(size_t* N, int64_t** arr, int64_t* X);
void echo_array(int64_t** arr, size_t N);
size_t count_x(int64_t** arr, size_t N, int64_t X);
int64_t nearest_x(int64_t** arr, size_t N, int64_t X);
void echo_indexes_x(int64_t** arr, size_t N, int64_t X);
int64_t get_max(int64_t** arr, size_t N);
size_t get_index_max(int64_t** arr, size_t N);

int main() {
    size_t N = 0;
    int64_t* array = NULL;
    int64_t X = 0;
    input(&N, &array, &X);
    if (N) {
        printf("%zu", get_index_max(&array, N));
        free(array);
    }
    return 0;
}

size_t get_index_max(int64_t** arr, size_t N) {
    int64_t max = LLONG_MIN;
    size_t i_max = 0;
    for (size_t i = 0; i < N; i++)
        if (max < (*arr)[i]) {
            max = (*arr)[i];
            i_max = i + 1;            
        }
    return i_max;
}

int64_t get_max(int64_t** arr, size_t N) {
    int64_t max = LLONG_MIN;
    for (size_t i = 0; i < N; i++)
        if (max < (*arr)[i]) max = (*arr)[i];
    return max;
}

void echo_indexes_x(int64_t** arr, size_t N, int64_t X) {
    for (size_t i = 0; i < N; i++)
        if (X == (*arr)[i]) printf("%zu%s", i + 1, " ");
}
    
int64_t nearest_x(int64_t** arr, size_t N, int64_t X) {
    int64_t item, delta = LLONG_MAX;
    for (size_t i = 0; i < N; i++)
        if (abs(X - (*arr)[i]) < delta) {
            item = (*arr)[i];
            delta = abs(X - (*arr)[i]);
        }
    return item;
}

size_t count_x(int64_t** arr, size_t N, int64_t X) {
    size_t count = 0;
    for (size_t i = 0; i < N; i++)
        if (X == (*arr)[i]) count++;
    return count;
}

void echo_array(int64_t** arr, size_t N) {
    for (size_t i = 0; i < N; i++)
        printf("%"PRId64"%s", (*arr)[i], i + 1 == N ? "" : " ");
}

void input(size_t* N, int64_t** arr, int64_t* X) {
    int64_t innum;
    *N = (scanf("%"SCNd64, &innum) == 1 && innum > 0) ? innum : 0;
    if (*N > 0) *arr = malloc(*N * sizeof(int64_t*));
    if (*arr == NULL) *N = 0;
    if (*N > 0)
        for (size_t i = 0; i < *N; i++)
            scanf("%"SCNd64, &((*arr)[i]));
    /* if (*N > 0) scanf("%"SCNd64, &(*X)); */
}


# C++
#include <iostream>

using namespace std;

int main()
{
    int N, max = -1001, max_i = 1, num;
    cin >> N;
    for(int i = 0; i < N; i++)
    {
        cin >> num;
        if(num > max)
        {
            max = num;
            max_i = i + 1;
        }
    }
    cout << max_i;
    return 0;
}






