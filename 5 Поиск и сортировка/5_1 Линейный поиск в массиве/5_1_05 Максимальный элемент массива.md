Максимальный элемент массива

Напишите программу, которая находит значение максимального элемента массива.

Входные данные

В первой строке задается одно натуральное число N, не превосходящее 1000 – размер массива.

Во второй строке вводятся N чисел – элементы массива (целые числа, не превосходящие по модулю 1000).

Выходные данные

Вывести одно число – значение максимального элемента в массиве.

Sample Input:

5
1 2 3 4 5
Sample Output:

5
Напишите программу. Тестируется через stdin → stdout
Верно решили 464 учащихся
Из всех попыток 75% верных


# ruby
n = gets.to_i
a = gets.chomp.split(' ').map { |j| j.to_i }
p a.max


# python
print([input(), max(list(map(int, input().split())))][1])


# JS
var stdin = process.openStdin();

stdin.addListener("data", function(d) {
    let str = d.toString().split('\n');
    str = str[1].split(' ');
    console.log(Math.max(...str));
  });


# C#
using System;
using System.Linq;

public class MainClass
{
    public static void Main()
    {
        string non=Console.ReadLine();
        int[] arr=Array. ConvertAll(Console.ReadLine().Split(), int.Parse);
        
        Console.WriteLine(arr.Max());
    }
}


# java
import java.util.Arrays;
import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
        Scanner input = new Scanner(System.in);
        input.nextLine();
        
        int[] array = Arrays.stream(input.nextLine().split("\\s+")).mapToInt(Integer::parseInt).toArray();

        System.out.println(Arrays.stream(array).max().getAsInt());
    }
}




# C
#include <inttypes.h>
#include <stdio.h>
#include <stdlib.h>
#include <limits.h>

void input(size_t* N, int64_t** arr, int64_t* X);
void echo_array(int64_t** arr, size_t N);
size_t count_x(int64_t** arr, size_t N, int64_t X);
int64_t nearest_x(int64_t** arr, size_t N, int64_t X);
void echo_indexes_x(int64_t** arr, size_t N, int64_t X);
int64_t get_max(int64_t** arr, size_t N);

int main() {
    size_t N = 0;
    int64_t* array = NULL;
    int64_t X = 0;
    input(&N, &array, &X);
    if (N) {
        printf("%"PRId64, get_max(&array, N));
        free(array);
    }
    return 0;
}

int64_t get_max(int64_t** arr, size_t N) {
    int64_t max = LLONG_MIN;
    for (size_t i = 0; i < N; i++)
        if (max < (*arr)[i]) max = (*arr)[i];
    return max;
}

void echo_indexes_x(int64_t** arr, size_t N, int64_t X) {
    for (size_t i = 0; i < N; i++)
        if (X == (*arr)[i]) printf("%zu%s", i + 1, " ");
}
    
int64_t nearest_x(int64_t** arr, size_t N, int64_t X) {
    int64_t item, delta = LLONG_MAX;
    for (size_t i = 0; i < N; i++)
        if (abs(X - (*arr)[i]) < delta) {
            item = (*arr)[i];
            delta = abs(X - (*arr)[i]);
        }
    return item;
}

size_t count_x(int64_t** arr, size_t N, int64_t X) {
    size_t count = 0;
    for (size_t i = 0; i < N; i++)
        if (X == (*arr)[i]) count++;
    return count;
}

void echo_array(int64_t** arr, size_t N) {
    for (size_t i = 0; i < N; i++)
        printf("%"PRId64"%s", (*arr)[i], i + 1 == N ? "" : " ");
}

void input(size_t* N, int64_t** arr, int64_t* X) {
    int64_t innum;
    *N = (scanf("%"SCNd64, &innum) == 1 && innum > 0) ? innum : 0;
    if (*N > 0) *arr = malloc(*N * sizeof(int64_t*));
    if (*arr == NULL) *N = 0;
    if (*N > 0)
        for (size_t i = 0; i < *N; i++)
            scanf("%"SCNd64, &((*arr)[i]));
    /* if (*N > 0) scanf("%"SCNd64, &(*X)); */
}



# C++
#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int main() {
int n;
cin>>n;
vector<int> vals(n);
for(int i=0;i<n;i++){
    cin>>vals[i];
}
cout<<*(max_element(vals.begin(),vals.end()));


