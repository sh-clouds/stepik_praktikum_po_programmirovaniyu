Матрица

Задана матрица K, содержащая n строк и m столбцов. Седловой точкой этой матрицы назовем элемент, который одновременно является минимумом в своей строке и максимумом в своем столбце.

Найдите количество седловых точек заданной матрицы.

Входные данные
Первая строка содержит целые числа n и m (1 ≤ n, m ≤ 750). Далее следуют n строк по m чисел в каждой. j-ое число i-ой строки равно kij. Все kij по модулю не превосходят 1000.

Выходные данные
Выведите ответ на задачу.

Sample Input:
2 2
0 0
0 0

Sample Output:
4

Напишите программу. Тестируется через stdin → stdout
Верно решили 225 учащихся
Из всех попыток 51% верных


# ruby
n,m = gets.chomp.split(' ').map { |j| j.to_i }
a = []
min_row, max_col,  = [], []
c = 0

0.upto(n-1) do |i|   
    a.push(gets.chomp.split(' ').map { |j| j.to_i }) 
    min_row.push(a[i].min)
end

b = a.transpose

b.each do |i|   
    max_col.push(i.max)
end

0.upto(n-1) do |i| 
    0.upto(m-1) do |j| 
        if a[i][j] == min_row[i] and a[i][j] == max_col[j]
            c += 1
        end    
    end
end

p c


# python
n, m = map(int, input().split())

matrix = [list(map(int, input().split())) for _ in range(n)]
matrix_rotate = list(zip(*matrix))

res = 0
for i in range(n):
    for j in range(m):
        if matrix[i][j] == max(matrix[i]) == min(matrix_rotate[j]):
            res += 1

print(res)


# JS
var stdin = process.openStdin();

stdin.addListener("data", function(d) {
    let str = d.toString().split('\n');
    let [arr, min, max] = [[], [], []]
    let result = 0;
    
    for (let i=1; i<str.length; i++) {
        let buf = str[i].split(' ');
        arr.push(buf);
        min.push(Math.min(...buf));
    }
    for (let j=0; j<arr[0].length; j++) {
        let buf = +arr[0][j];
        for (let i=0; i<arr.length; i++) 
            if ( buf < +arr[i][j] ) buf = +arr[i][j]
        max.push(buf);        
    }
    for (let i=0; i<min.length; i++) 
        for (let j=0; j<max.length; j++) 
            if (min[i] === max[j] ) result++;        
    console.log(result);
  });

  
# C#
using System;
using System.Linq;

public class MainClass
{
    public static void Main()
    {
        int[] nm=Array.ConvertAll(Console.ReadLine().Split(),int.Parse);
        int n = nm[0];
        int m = nm[1];
        int[][] arr=new int[n][];
        int[] min = new int[n];
        bool isPoint =false;
        int count = 0;

        for(int y=0; y<n; y++)
        {
            arr[y]= Array.ConvertAll(Console.ReadLine().Split(), int.Parse);
            min[y] = arr[y].Min(); 
        }
        for(int y=0; y<n;y++)
        {
            for(int x=0;x<m;x++)
            {
                if (arr[y][x] == min[y])
                    isPoint = true;
                {
                    for(int i=0;i<n;i++)
                    {
                        if (arr[i][x] > min[y])
                        {
                            isPoint = false;
                            break;
                        }                        
                    }
                    if (isPoint)
                        count++;
                }
            }
         }
        Console.WriteLine(count);
    }
}


# java
import java.util.Arrays;
import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
        Scanner input = new Scanner(System.in);
        int row = input.nextInt(), col = Integer.parseInt(input.nextLine().trim()), counter = 0;

        int[][] matrix = new int[row][col];

        for(int a = 0; a < row; a++) {
            for(int b = 0; b < col; b++) {
                matrix[a][b] = input.nextInt();
            }
        }

        for(int e = 0; e < row; e++) {
            int min = Arrays.stream(matrix[e]).min().getAsInt();
            int max = Integer.MIN_VALUE;

            for(int f = 0; f < col; f++) {
                for(int g = 0; g < row; g++) {
                    if(matrix[g][f] > max) max = matrix[g][f];
                }
                if(min == max) ++counter;
            }
        }

        System.out.println(counter);
    }
}



# C
#include <inttypes.h>
#include <stdio.h>
#include <stdlib.h>
#include <limits.h>

void input(size_t* N, size_t* M);
int8_t init_matrix(int64_t*** arr, size_t N, size_t M);
void free_matrix(int64_t*** arr, size_t N);
void input_matrix(int64_t*** arr, size_t N, size_t M);
int64_t get_min_in_row(int64_t*** arr, size_t M, size_t i);
int64_t get_max_in_col(int64_t*** arr, size_t N, size_t j);
size_t count_saddle_point(int64_t*** arr, size_t N, size_t M);

int main() {
    size_t N = 0, M = 0;
    int64_t** matrix = NULL;
    input(&N, &M);
    if (N && M && init_matrix(&matrix, N, M)) {
        input_matrix(&matrix, N, M);
        printf("%zu", count_saddle_point(&matrix, N, M));
        free_matrix(&matrix, N);
    }
    return 0;
}

size_t count_saddle_point(int64_t*** arr, size_t N, size_t M) {
    size_t count = 0;
    for (size_t i = 0; i < N; i++)
        for (size_t j = 0; j < M; j++)
            if ((*arr)[i][j] == get_min_in_row(&(*arr), N, i) &&
                (*arr)[i][j] == get_max_in_col(&(*arr), N, j))
                count++;
    return count;
}
    
int64_t get_max_in_col(int64_t*** arr, size_t N, size_t j) {
    int64_t max = LLONG_MIN;
    for (size_t i = 0; i < N; i++)
        if ((*arr)[i][j] > max)
            max = (*arr)[i][j];
    return max;
}
   
int64_t get_min_in_row(int64_t*** arr, size_t M, size_t i) {
    int64_t min = LLONG_MAX;
    for (size_t j = 0; j < M; j++)
        if ((*arr)[i][j] < min)
            min = (*arr)[i][j];
    return min;
}

void input_matrix(int64_t*** arr, size_t N, size_t M) {
    for (size_t i = 0; i < N; i++)
        for (size_t j = 0; j < M; j++)
            scanf("%"SCNd64, &((*arr)[i][j]));
}

void free_matrix(int64_t*** arr, size_t N) {
    for (size_t i = 0; i < N; i++) free((*arr)[i]);
    free(*arr);
}

int8_t init_matrix(int64_t*** arr, size_t N, size_t M) {
    int8_t result = 0;
    *arr = malloc(N * sizeof(int64_t*));
    if (*arr != NULL) {
        result = 1;
        for (size_t i = 0; i < N; i++) {
            (*arr)[i] = malloc(M * sizeof(int64_t));
            if ((*arr)[i] == NULL) {
                result = 0;
                free_matrix(&(*arr), i);
                break;
            }
        }
    }
    return result;
}

void input(size_t* N, size_t* M) {
    int64_t num;
    *N = (scanf("%"SCNd64, &num) == 1 && num > 0) ? num : 0;
    *M = (scanf("%"SCNd64, &num) == 1 && num > 0) ? num : 0;
}



# C++
#include <iostream>
#include <vector>
#include <limits.h>

int main()
{
    int n, m, max = INT_MIN, k = 0, k1 = 0, res = 0, min = INT_MAX;
    std::cin >> n >> m;
    std::vector <std::vector <int>> a(n, std::vector <int> (m));
    for(int i = 0; i < n; i++)
    {
        for(int j = 0; j < m; j++)
        {
            std::cin >> a[i][j];
        }
    }
    std::vector <int> b(n);
    std::vector <int> c(m);
    int o = 0, o1 = 0;
    for(int i = 0; i < n; i++)
    {
        min = INT_MAX, k = 0;
        for(int j = 0; j < m; j++)
        {
            if(a[i][j] < min)
            {
                min = a[i][j];
                k = j;
            }
        }
        b[o] = k;
        o++;
    }
    for(int j = 0; j < m; j++)
    {
            max = INT_MIN, k1 = 0;
            for(int i = 0; i < n; i++)
            {
                if(a[i][j] > max)
                {
                        max = a[i][j];
                        k1 = i;
                }
            }
            c[o1] = k1;
            o1++;
    }
    for(int i = 0; i <  o; i++)
    {
            for(int j = 0; j < o1; j++)
            {
                    if(a[i][j] == a[c[i]][b[j]])
                    {
                        res++;
                    }
            }
    }
    std::cout << res;
    return 0;
}




