Конь

Требуется определить, бьет ли конь, стоящий на клетке с указанными координатами (номер строки и номер столбца), фигуру, стоящую на другой указанной клетке.

Входные данные
Вводятся четыре числа: координаты коня и координаты другой фигуры. Все координаты - целые числа в интервале от 1 до 8.

Выходные данные
Программа должна вывести слово YES, если конь может побить фигуру за 1 ход, в противном случае вывести слово NO.

Sample Input:
1
1
3
2

Sample Output:
YES

Напишите программу. Тестируется через stdin → stdout
Верно решили 807 учащихся
Из всех попыток 73% верных


# ruby
x1,y1,x2,y2 = gets.to_i,gets.to_i,gets.to_i,gets.to_i
#abs((x1 - x2) * (y1 - y2)) == 2
puts ((x1 - x2) * (y1 - y2)).abs == 2 ? "YES" : "NO"


# python
x, y, x1, y1 = (int(input()) for _ in range(4))

print('YES' if (x1 - x) ** 2 + (y1 - y) ** 2 == 5 else'NO')


# JS
process.stdin.resume();
process.stdin.setEncoding("ascii");

let inputStdin = "";
let inputStdinArray = "";
let inputCurrentline = 0;

process.stdin.on("data", function (data) {
  inputStdin += data;
});

process.stdin.on("end", function () {
  inputStdinArray = inputStdin.split("\n");
  main();
});

function readLine() {
  return inputStdinArray[inputCurrentline++];
}
       
function main() {
   let a1 = readLine(),
       a2 = readLine(),
       b1 = readLine(),
       b2 = readLine(),
       aa = a1 + a2,
       bb = b1 + b2;
       Number(aa);
       Number(bb);
       let cc = Math.abs(aa - bb);
    
        switch (cc) {
          case 8:
            console.log('YES');
            break;
          case 12:
            console.log('YES');
            break;
          case 19:
            console.log('YES');
            break;
          case 21:
            console.log('YES');
            break;
          default:
            console.log('NO');
        }
    }


# C#
using System;

public class MainClass
{
    public static void Main()
    {
        int x1 = int.Parse(Console.ReadLine());
        int y1 = int.Parse(Console.ReadLine());
        int x2 = int.Parse(Console.ReadLine());
        int y2 = int.Parse(Console.ReadLine());
        if((Math.Pow((x1 - x2), 2) + Math.Pow((y1 - y2), 2)) == 5)
            Console.WriteLine("YES");
        else
            Console.WriteLine("NO");
    }
}


# java
import java.util.Scanner;

class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int a1 = sc.nextInt();
        int a2 = sc.nextInt();
        int b1 = sc.nextInt();
        int b2 = sc.nextInt();
        if (Math.abs(a1 - b1) <= 2 && Math.abs(a2 - b2) <= 1 || Math.abs(a1 - b1) <= 1 && Math.abs(a2 - b2) <= 2) {
            System.out.println("YES");
        } else {
            System.out.println("NO");
        }
    }
}


# C
#include <stdio.h>
#include <stdlib.h>

int main() {
    int x, y, X, Y;
    scanf("%d %d %d %d", &x, &y, &X, &Y);
    
    int res = abs(x - X) * abs(y - Y) == 2;
        
    puts(res ? "YES" : "NO");
    return 0;
}


# C++
#include <iostream>

int main() {
    int x1,y1,x2,y2;
    std::cin >> x1 >> y1 >> x2 >> y2;
    std::cout << ( (abs((x1 - x2) * (y1 - y2)) == 2) ? "YES" : "NO" ) << "\n";
    return 0;
}




