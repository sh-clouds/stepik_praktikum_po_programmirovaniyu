Геометрическая прогрессия

По данным натуральным числам a и n вычислите сумму 1+a+a2+...+an, не используя формулу суммы геометрической прогрессии. Время работы программы должно быть пропорционально n.

Входные данные
Вводятся 2 числа - a и n.

Выходные данные
Необходимо вывести  значение суммы.

Sample Input:
2
2

Sample Output:
7

Напишите программу. Тестируется через stdin → stdout
Верно решили 596 учащихся
Из всех попыток 32% верных


# ruby
a, n = gets.to_i, gets.to_i
b = 0
0.upto(n) do |i|
    b = b + a.pow(i)
end

puts b


# python
a, n = [int(input()) for i in range(2)]
sum = 1
for i in range(1, n+1):
    sum+=a**i
print(sum) 


# JS
var stdin = process.openStdin();
stdin.addListener("data", function(d) {
    let data = d.toString().split('\n');
    let a = +data[0];
    let n = +data[1];
    let result = getResult(a, n);
    console.log(result.toString());
  });

function getResult(a, n){
    a = BigInt(a);
    n = BigInt(n);
    if(n == 0) return 1n;
    if(n == 1) return a + 1n;
    return 1n + a * (getResult(a, n - 1n));
}


# C#
using System;
using System.Numerics;

public class MainClass
{
    public static void Main()
    {
       Input(out long a);
       Input(out long n);
       long sum=1;
       for (int i=1;i<=n;i++)
       {
           sum+=(long)Pow(a,i);
       }
      Console.WriteLine(sum);
    }
    static void Input(out long x)
    {
        x=long.Parse(Console.ReadLine());
    }
    static long Pow(long x, long y)
    {
        long f=1;
        for (int i=1;i<=y;i++)
        {
            f*=x;
        }
        return f;
    }
}


# java
import java.util.Scanner;
import java.math.BigInteger;

class Main {
    public static void main(String[] args) {


        Scanner scan = new Scanner(System.in);

        BigInteger a = new BigInteger(scan.nextLine());
        int n = scan.nextInt();
        scan.close();

        BigInteger sumOfSqr = BigInteger.ONE;

        for (int i = 1; i <= n; i++) {
            sumOfSqr = sumOfSqr.multiply(a).add(BigInteger.ONE);
        }

        System.out.println(sumOfSqr);

    }

}


# C
#include <stdio.h>
int main() {
    for (unsigned long long a,n,r=1,i=(scanf("%llu%llu",&a,&n),0); i<n || (printf("%llu\n",r),0); )        
        for (unsigned long long p=1,j=++i; j || (r+=p,0); p*=a, --j);
    return 0;
}


# C++
#include <iostream>

int main() {
    long long a, n, s = 0, p = 1;
    std::cin >> a >> n;
    while (n-- >= 0) {
        s += p;
        p *= a;
    }
    std::cout << s << "\n";
    return 0;
}




