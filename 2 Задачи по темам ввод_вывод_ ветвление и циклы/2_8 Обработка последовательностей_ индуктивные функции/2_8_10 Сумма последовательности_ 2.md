Сумма последовательности - 2

Найдите сумму последовательности натуральных чисел, если признаком окончания конца последовательности является два подряд идущих числа 0.

Числа, следующие после двух подряд идущих нулей считывать не нужно.

Входные данные
Вводится последовательность целых чисел, оканчивающаяся числом 0 (само число 0 в последовательность не входит).

Выходные данные
Выведите ответ на задачу.

Sample Input:
1
0
7
0
9
0
0

Sample Output:
17

Напишите программу. Тестируется через stdin → stdout
Верно решили 600 учащихся
Из всех попыток 59% верных


# ruby
nums = $stdin.read.split("\n").map(&:to_i)
#nums = [1,1,1,0,0,2,3]
cnt = 0
nums.each_cons(2) do |a|
    if a == [0,0]
        puts nums[..cnt].sum
        break
    end
    cnt += 1
end


# python
total = 0
zero_qty = 0

while True:
    cur = int(input())
    if not cur and zero_qty:
        break
    elif not cur:
        zero_qty = 1
    else:
        zero_qty = 0
        total += cur
        
print(total)


# JS
var stdin = process.openStdin();

stdin.addListener("data", function(d) {
    let data = d.toString().split('\n');
    let i = 0;
    let count = 0;

     while ( +data[i] + +data[i+1] !== 0) { 
         count = count + +data[i];
         i++;
    }

    console.log(count);
  });

  
# C#
using System;

public class MainClass
{
    public static void Main()
    {
        bool check=true;
        int sum=0;
        int nul=0;
        int i=0;
        while(check)
        {
         i=int.Parse(Console.ReadLine());
            if (i==0)
            {
              nul++;
                if(nul==2)
                check=false;
            }
            else
            {
                sum+=i;
                nul=0;
            }
        }
        Console.WriteLine(sum);
    }
}


# java
import java.util.Scanner;

class Main {
    public static void main(String[] args) {
        Scanner input = new Scanner(System.in);
        int sum = 0, counter = 0;

        while(true) {
            int current = input.nextInt();

            if(current > 0) {
                counter = 0;
            }
            else {
                ++counter;
            }

            if(counter == 2) {
                break;
            }

            sum += current;
        }

        System.out.println(sum);
    }
}


# C
#include <stdio.h>
int main() {
    for (int a,b,s=(scanf("%d%d",&a,&b),a); a!=0 || b!=0 || (printf("%d\n",s),0); s+=b, a=b, scanf("%d",&b));
    return 0;
}


# C++
#include <iostream>

int main() {
    int n, s = 0, cnt_0 = 0;
    std::cin >> n;
    while (cnt_0 != 2) {
        if (n == 0) cnt_0++;
        else cnt_0 = 0;
        s += n;
        std::cin >> n;
    }
    std::cout << s << "\n";
    return 0;
}







