Максимальное число идущих подряд равных элементов

Дана последовательность натуральных чисел, завершающаяся числом 0. Определите, какое наибольшее число подряд идущих элементов этой последовательности равны друг другу. Если все элементы разные, максимальным числом будет 1.

Числа, следующие за числом 0, считывать не нужно.

Входные данные
Дана последовательность натуральных чисел, завершающаяся числом 0.

Выходные данные
Выведите ответ на задачу.

Sample Input 1:
1
7
7
9
1
0

Sample Output 1:
2

Sample Input 2:
1
2
3
0

Sample Output 2:
1

Напишите программу. Тестируется через stdin → stdout
Верно решили 554 учащихся
Из всех попыток 38% верных



# put your ruby code here
a = (nums = $stdin.read.split("\n").map(&:to_i))[...(nums.find_index(0))]
cnt = 0
cnt_max = 0
nums.each_cons(2) do |v|
    if v[0] == v[1]
        cnt += 1 
        if cnt_max < cnt
            cnt_max = cnt
        end    
    else
        cnt = 0 
    end
end
puts cnt_max + 1


# put your python code here
prev = 0
len_cur = len_mx = 0

for cur in map(int, iter(input, '0')):
    if cur == prev:
        len_cur += 1
    else:
        if len_cur > len_mx:
            len_mx = len_cur
        len_cur = 1
        prev = cur
        
print(max(len_mx, len_cur))


# JS
let arr = require('fs').readFileSync(0, 'utf8').split('\n');

arr = arr.slice(0, arr.indexOf('0')+1);

let [tmp, res] = [arr[0], 1];
for (let i = 1; i < arr.length; i++) {
  if (arr[i-1] != arr[i]) {
    res = (res < tmp.length) ? tmp.length : res;
    tmp = '';
  }
  tmp += arr[i];
}

console.log(res);


# C#
using System;

public class MainClass
{
    public static void Main()
    {
      int prev = -1;
      int curr_rep_len = 0;
      int max_rep_len = 0;
      int element;
while ((element = int.Parse(Console.ReadLine()))!= 0)
{
    if (prev == element)
        curr_rep_len += 1;
    else
    {
        prev = element;
        max_rep_len = Math.Max(max_rep_len, curr_rep_len);
        curr_rep_len = 1;
     }
max_rep_len = Math.Max(max_rep_len, curr_rep_len);
}
Console.WriteLine(max_rep_len);
    }
}

# java
import java.util.Scanner;

class Main {
    public static void main(String[] args) {
        Scanner input = new Scanner(System.in);
        int previous = input.nextInt(), current, counter = 1, max = 1;

        while(true) {
            current = input.nextInt();

            if(current == 0) {
                if(counter > max) {
                    max = counter;
                }
                break;
            }

            if(previous == current) {
                ++counter;
            }
            else {
                if(counter > max) {
                    max = counter;
                }
                counter = 1;
            }

            previous = current;
        }

        System.out.println(max);
    }
}


# C++
#include <iostream>

int main() {
    int n, old = 0, i = 0, max_i = 0;
    std::cin >> n;
    old = n;
    while (n != 0) {
        if (n == old) i++;
        else i = 1;
        if (i > max_i) max_i = i;
        old = n;
        std::cin >> n;
    }
    std::cout << max_i << "\n";
    return 0;
}


# C
#include <stdio.h>
int main() {
    int x,a=(scanf("%d",&x),x),c=1,mx=1;
    while (scanf("%d",&x)==1 && x) 
        a==x ? ++c : (c>mx ? mx=c : 0, a=x, c=1);    
    c>mx ? mx=c : 0;
    printf("%d\n",mx);
    return 0;
}





