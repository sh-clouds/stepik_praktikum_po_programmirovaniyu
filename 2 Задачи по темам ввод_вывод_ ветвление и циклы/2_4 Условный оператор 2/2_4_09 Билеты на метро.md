Билеты на метро

Билет на одну поездку в метро стоит 15 рублей, билет на 10 поездок стоит 125 рублей, билет на 60 поездок стоит 440 рублей. Пассажир планирует совершить n поездок. Определите, сколько билетов каждого вида он должен приобрести, чтобы суммарное количество оплаченных поездок было не меньше n, а общая стоимость приобретенных билетов – минимальна.

Входные данные
Дано одно число n - количество поездок.

Выходные данные
Выведите три целых числа, равные необходимому количеству билетов на 1, на 10, на 60 поездок.

Sample Input:
129

Sample Output:
0 1 2

Напишите программу. Тестируется через stdin → stdout
Верно решили 552 учащихся
Из всех попыток 39% верных



# ruby
#1 - 15
#10 - 125
#60 - 440

n = gets.to_i
a,b,c = 0,0,0
nmin1,nmin2,nmin3,nmin4 = 0,0,0,0
vmin1,vmin2,vmin3,vmin4 = 0,0,0,0

a = n / 60
if n % 60 != 0
    nmin1 = (a+1)*60
    vmin1 = (a+1)*440
    n = n - a * 60
    b = n / 10
    
    if n % 10 == 0
        nmin2 = a*60 + b*10
        vmin2 = a*440 + b*125 
        if vmin1 <= vmin2
            a = a+1
            b=0
        end    
    else  
        c = n - b * 10
        nmin3 = a*60 + (b+1)*10
        vmin3 = a*440 + (b+1)*125
        
        nmin4 = a*60 + b*10 + c
        vmin4 = a*440 + b*125 + c*15 
        
        if vmin1 <= vmin3
           if vmin1 <= vmin4
               a = a+1
               b=0
               c=0
           end
        else
           if vmin3 <= vmin4
               b=b+1
               c=0 
           end
        end 
    end
end

print "#{c} #{b} #{a}"


# python
n = int(input())
sixty, ten, one, n = n // 60, 0, 0, n % 60
if (n // 10) * 125 + ((n % 10) // 1) * 15 > 440: sixty, n = sixty + 1, n - 60
else: ten, n = n // 10, n % 10
if n > 0 and n * 15 > 125: ten += 1
elif n > 0: one = n
print(one, ten, sixty)


# JS
let trips = Number(require('fs').readFileSync(0, 'utf8'));
let seasonTicket1 = 0, seasonTicket10 = 0, seasonTicket60 = 0;

if (trips >= 60) {
    seasonTicket60 = Math.floor(trips / 60);
    trips -= seasonTicket60 * 60;
}
if (trips % 60 > 34) {
    seasonTicket60++;
    trips -= 60;
}
if (trips > 0) {
    if (trips % 10 > 8) {
        seasonTicket10 = Math.ceil(trips / 10);
    } else {
        seasonTicket10 = Math.floor(trips / 10);
    }
    trips -= seasonTicket10 * 10;
    seasonTicket1 = trips >= 0 ? trips : 0;
}

console.log(seasonTicket1, seasonTicket10, seasonTicket60);


# C#
using System;

public class MainClass
{
    public static void Main()
    {
   int n = int.Parse(Console.ReadLine());
   int a,b,c;
   a = n / 60;
   n = n%60;
   b=n/10;
   c=n%10;
    if (c * 15 > 125)
    {
       c = 0 ;
       b ++;
    }
       if (c * 15 + b * 125 > 440)
       {
        c = b = 0;
        a += 1;
        }
         Console.WriteLine(c+" "+b+" "+a);
    }
}


# java
import java.util.Scanner;

public class Main {

    public static void main(String[] args) {
        Scanner in = new Scanner(System.in);
        int a = in.nextInt();
        in.close();
        int x60 = a / 60;
        int x1 = 0;
        int x10 = 0;
        if (a % 60 > 34) {
            x60++;
        } else if (x60 * 60 < a) {
            if ((a % 60 ) % 10 > 8) {
                x10 = (a %60) / 10 + 1;
            } else {
                x10 = (a % 60 ) / 10;
                x1 = a % 10;
            }
        }
        System.out.println(x1 + " " + x10 + " " + x60);
    }
}


# C
#include <stdio.h>
int main() {
    for (int n; scanf("%d",&n)==1; printf("%d %d %d\n", 
        n%60/35 ? 0 : n%60%10%9,         
        n%60/35 ? 0 : (n+1)%60/10, 
        (n+25)/60));
    return 0;
}


# C++
#include <iostream>

int main() {
    int n, p1, p10, p60;
    std::cin >> n;
    p60 = n / 60;
    n %= 60;
    p10 = n / 10;
    n %= 10;
    p1 = n / 1;
    if (p1 * 15 + p10 * 125 >= 440) {
        p1 = 0;
        p10 = 0;
        ++p60;
    }
    if (p1 >= 9) {
        p1 = 0;
        ++p10;
    }
    if (p10 >= 4) {
        p10 = 0;
        ++p60;
    }
    std::cout << p1 << " " << p10 << " " << p60 << "\n";
    return 0;
}









