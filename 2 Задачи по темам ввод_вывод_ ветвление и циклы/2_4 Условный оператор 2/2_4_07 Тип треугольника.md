Тип треугольника

Определите тип треугольника (остроугольный, тупоугольный, прямоугольный) с данными сторонами.

Входные данные
Даны три натуральных числа – стороны треугольника.

Выходные данные
Необходимо вывести одно из слов: right для прямоугольного треугольника, acute для остроугольного треугольника, obtuse для тупоугольного треугольника или impossible, если входные числа не образуют треугольника.

Sample Input:
3
4
5

Sample Output:
right

Напишите программу. Тестируется через stdin → stdout
Верно решили 614 учащихся
Из всех попыток 28% верных



'''
impossible, если входные числа не образуют треугольника.
right для прямоугольного треугольника, 
obtuse для тупоугольного треугольника,
acute для остроугольного треугольника 

1. Проверка на существование треугольника
2. Проверка на Прямоугольный тип
3. Проверка на Тупоугольный тип
4. Проверка на Остроугольный тип
'''
   
# ruby   
a,b,c = gets.to_i,gets.to_i,gets.to_i
#a,b,c = 2,4,0

def is_exist? a,b,c
    if a+b>c && b+c>a && a+c>b
        return true  
    elsif (a-b).abs<c && (b-c).abs<a && (a-c).abs<b
        return true 
    else 
        return false  
    end  
end    

def is_right? a,b,c
    if a*a+b*b==c*c || c*c+b*b==a*a || a*a+c*c==b*b
        return true  
    else 
        return false  
    end  
end

def is_obtuse? a,b,c
    if a*a+b*b<c*c || c*c+b*b<a*a || a*a+c*c<b*b
        return true  
    else 
        return false  
    end  
end

def is_acute? a,b,c
    if a*a+b*b>c*c || c*c+b*b>a*a || a*a+c*c>b*b
        return true  
    else 
        return false  
    end  
end

if !is_exist? a,b,c
    puts 'impossible'
elsif is_right? a,b,c  
    puts 'right'
elsif is_obtuse? a,b,c   
    puts 'obtuse'    
elsif is_acute? a,b,c   
    puts 'acute'
end    


# python
a, b, c = [int(input()) for i in range(3)]

if a+b<=c or b+c<=a or c+a<=b:
    print('impossible')
elif a**2==b**2+c**2 or b**2==a**2+c**2 or c**2==b**2+a**2:
    print('right')
elif a**2<b**2+c**2 and b**2<a**2+c**2 and c**2<b**2+a**2:
    print('acute')
elif a**2>b**2+c**2 or b**2>a**2+c**2 or c**2>b**2+a**2:
    print('obtuse')


# JS
var stdin = process.openStdin();
stdin.addListener("data", function(e) {
    let data = e.toString().split('\n');
    let a = +data[0];
    let b = +data[1];
    let c = +data[2];
    getResult(a, b, c);
});

function getResult(a, b, c){
    if(a + b > c && a + c > b && b + c > a){
        
        let arr = [a, b, c];
        let max = Math.max(a, b, c);
        let newArr = arr.filter((n) => {return n != max});
        
        if(max ** 2 > newArr[0] ** 2 + newArr[1] ** 2){
            console.log('obtuse');
        }else if(max ** 2 == newArr[0] ** 2 + newArr[1] ** 2){
            console.log('right');
        }else if(max ** 2 < newArr[0] ** 2 + newArr[1] ** 2){
            console.log('acute');
        }
    }else{
        console.log('impossible');
    }
}


# C#
using System;

public class MainClass
{
    public static void Main()
    {
            int a = int.Parse(Console.ReadLine());
            int b = int.Parse(Console.ReadLine());
            int c = int.Parse(Console.ReadLine());
            int max, another1, another2;
            if (a > b && a > c)
            {
                max = a;
                another1 = b;
                another2 = c;
            } else if (b > a && b > c)
            {
                max = b;
                another1 = a;
                another2 = c;
            }
            else
            {
                max = c;
                another1 = b;
                another2 = a;
            }
            if (a + b > c && b + c > a && c + a > b)
            {
                if (max*max == another1*another1 + another2*another2)
                {
                    Console.WriteLine("right");
                }else if (max*max < another1*another1+another2*another2)
                {
                    Console.WriteLine("acute");
                }
                else
                {
                    Console.WriteLine("obtuse");
                }
            }
            else
            {
                Console.WriteLine("impossible");
            }
    }
}


# java
import java.util.Scanner;

class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int a = sc.nextInt();
        int b = sc.nextInt();
        int c = sc.nextInt();
        int idk1 = Math.max(a, b);
        int max = Math.max(idk1, c);
        int idk2 = Math.min(a, b);
        int min = Math.min(idk2, c);
        int mid = a + b + c - max - min;
        if (a + b > c && a + c > b && b + c > a) {
            if ((max * max) == (mid * mid + min * min)) {
                System.out.println("right");
            } else if ((max * max) < (mid * mid + min * min)) {
                System.out.println("acute");
            } else {
                System.out.println("obtuse");
            }
        } else {
            System.out.println("impossible");
        }
    }
}


# C
#include <stdio.h>
void sort(int *a, int *b, int *c) {
    int x;
    *a>*b ? x=*a, *a=*b, *b=x : 0;
    *b>*c ? x=*b, *b=*c, *c=x : 0;
    *a>*b ? x=*a, *a=*b, *b=x : 0;
}
int main() {
    for (int a,b,c,x,y; scanf("%d%d%d",&a,&b,&c)==3; ) {
        sort(&a,&b,&c);
        x=c*c; y=a*a+b*b;
        puts((char *[]){"impossible","acute","right","obtuse"}[(c<a+b)*(1+(x>=y)+(x>y))]);
    }
    return 0;
}


# C++
#include <iostream>
#include <string>
#include <cmath>

bool is_exist(int a, int b, int c)
{
    if (a+b>c && b+c>a && a+c>b) return true;  
    else if (abs(a-b)<c && abs(b-c)<a && abs(a-c)<b) return true; 
    else return false;
}

bool is_right(int a, int b, int c)
{
    if (a*a+b*b==c*c || c*c+b*b==a*a || a*a+c*c==b*b) return true;  
    else return false;  
}

bool is_obtuse(int a, int b, int c)
{
    if (a*a+b*b<c*c || c*c+b*b<a*a || a*a+c*c<b*b) return true;  
    else return false;  
}

bool is_acute(int a, int b, int c)
{
    if (a*a+b*b>c*c || c*c+b*b>a*a || a*a+c*c>b*b) return true;  
    else return false; 
}
 
int main() {
    int a, b, c;
    std::cin >> a >> b >> c;
    std::string output="";
    if      (!is_exist(a,b,c)) output = "impossible";  
    else if  (is_right(a,b,c)) output = "right";
    else if (is_obtuse(a,b,c)) output = "obtuse";    
    else if  (is_acute(a,b,c)) output = "acute";       
    std::cout << output + "\n";
    return 0;
}







