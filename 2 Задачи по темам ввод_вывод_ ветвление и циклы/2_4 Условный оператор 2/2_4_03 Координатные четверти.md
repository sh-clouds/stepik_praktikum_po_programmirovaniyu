Координатные четверти

Даны координаты двух точек на плоскости, требуется определить, лежат ли они в одной координатной четверти или нет (все координаты отличны от нуля).

Входные данные
Вводятся 4 числа: координаты первой точки (x1, y1) и координаты второй точки (x2, y2).

Выходные данные
Программа должна вывести слово YES, если точки находятся в одной координатной четверти, в противном случае вывести слово NO.

Sample Input:
3
3
5
1

Sample Output:
YES

Напишите программу. Тестируется через stdin → stdout
Верно решил 651 учащийся
Из всех попыток 60% верных


# ruby
x1,y1,x2,y2 = gets.to_i,gets.to_i,gets.to_i,gets.to_i
#puts x1,y1,x2,y2
if x1>0 && x2>0 && y1>0 && y2>0 
    puts "YES"   
elsif x1>0 && x2>0 && y1<0 && y2<0 
    puts "YES"
elsif x1<0 && x2<0 && y1>0 && y2>0
    puts "YES" 
elsif x1<0 && x2<0 && y1<0 && y2<0
    puts "YES" 
else puts "NO" 
end 


# python
x1,y1,x2,y2=[int(input())for _ in '....']
print(['NO','YES'][x1*x2>0 and y1*y2>0])


# JS
let [x1, y1, x2, y2] = require("fs")
  .readFileSync(0, "utf8")
  .split("\n")
  .map(Number);

const res = (x1 * x2 > 0 && y1 * y2 > 0) ? "YES" : "NO";
console.log(res);


# C#
using System;

public class MainClass
{
    public static void Main()
    {
            int x = int.Parse(Console.ReadLine());
            int y = int.Parse(Console.ReadLine());
            int x1 = int.Parse(Console.ReadLine());
            int y1 = int.Parse(Console.ReadLine());
            if (x > 0 && y > 0 && x1 > 0 && y1 > 0)
            {
                Console.WriteLine("YES");
            }
            else if (x > 0 && x1 > 0 && y < 0 && y1 < 0)
            {
                Console.WriteLine("YES");
            }
            else if (x < 0 && x1 < 0 && y > 0 && y1 > 0)
            {
                Console.WriteLine("YES");
            }
            else if (x < 0 && x1 < 0 && y < 0 && y1 < 0)
            {
                Console.WriteLine("YES");
            }
            else
            {
                Console.WriteLine("NO");
            }
    }
}


# java
import java.util.Scanner;
class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int x1 = sc.nextInt();
        int y1 = sc.nextInt();
        System.out.print( x1 * sc.nextInt() > 0 && y1 * sc.nextInt() > 0 ? "YES" : "NO");
        sc.close();
    }
}


# C
#include <stdio.h>
int main() {
    for (int x,y,X,Y; scanf("%d%d%d%d",&x,&y,&X,&Y)==4; puts((x>0)==(X>0) && (y>0)==(Y>0) ? "YES" : "NO"));
    return 0;
}


# C++
#include <iostream>
#include <string>

int main() {
    int x1,y1,x2,y2;
    std::cin >> x1 >> y1 >> x2 >> y2;
    std::string output;
    
    if (x1>0 && x2>0 && y1>0 && y2>0)
    {
        output = "YES\n";
    }
    else if (x1>0 && x2>0 && y1<0 && y2<0) 
    {
        output = "YES\n";
    }
    else if (x1<0 && x2<0 && y1>0 && y2>0)
    {
        output = "YES\n";
    }  
    else if (x1<0 && x2<0 && y1<0 && y2<0)
    {
        output = "YES\n";
    }      
    else
    {
        output = "NO\n";
    }    
        
    std::cout << output;
    return 0;
}





