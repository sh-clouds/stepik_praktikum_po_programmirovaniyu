Билеты на метро - 2

Билет на одну поездку в метро стоит 15 рублей, билет на 5 поездок стоит 70 рублей, билет на 10 поездок стоит 125 рублей, билет на 20 поездок стоит 230 рублей, билет на 60 поездок стоит 440 рублей. Пассажир планирует совершить n поездок. Определите, сколько билетов каждого вида он должен приобрести, чтобы суммарное количество оплаченных поездок было не меньше n, а общая стоимость приобретенных билетов – минимальна.

Входные данные
Дано одно число n - количество поездок.

Выходные данные
Выведите пять целых чисел, равные необходимому количеству билетов на 1, на 5, на 10, на 20, на 60 поездок. Если для какого-то данного n существует несколько способов приобретения билетов одинаковой стоимости, необходимо вывести ту комбинацию билетов, которая дает большее число поездок.

Sample Input:
1

Sample Output:
1 0 0 0 0

Напишите программу. Тестируется через stdin → stdout
Верно решили 424 учащихся
Из всех попыток 31% верных



# ruby
#1 - 15
#5 - 70 vmin7, vmin8
#10 - 125 vmin5, vmin6
#20 - 230 vmin3, vmin4
#60 - 440 vmin1, vmin2

n = gets.to_i
#n = 14
a,b,c,d,e = 0,0,0,0,0
a_v,b_v,c_v,d_v,e_v = 440, 230, 125, 70, 15
nmin1,nmin2,nmin3,nmin4,nmin5,nmin6,nmin7,nmin8 = 0,0,0,0,0,0,0,0
vmin1,vmin2,vmin3,vmin4,vmin5,vmin6,vmin7,vmin8 = 0,0,0,0,0,0,0,0

a = n / 60
if n % 60 != 0
    n = n - a*60
    b = n / 20  
    
    nmin1 = (a+1)*60
    vmin1 = (a+1)*a_v
    
    nmin2 = a*60 + b*20
    vmin2 = a*a_v + b*b_v 
    
    if n % 20 == 0
        if vmin1 <= vmin2
            a = a+1
            b=0
            c=0
            d=0
            e=0
            #print "vmin1 <= vmin8\2"
        end    
    else  
        n = n - b*20
        c = n / 10  
        
        nmin3 = a*60 + (b+1)*20
        vmin3 = a*a_v + (b+1)*b_v 
        
        nmin4 = a*60 + b*20 + c*10
        vmin4 = a*a_v + b*b_v + c*c_v 
        
        if n % 10 == 0
            if vmin1 <= vmin3
                if vmin1 <= vmin4
                    a = a+1
                    b=0
                    c=0
                    d=0
                    e=0
                    #print "vmin1 <= vmin4\n"
                end
            else
                if vmin3 <= vmin4
                    b=b+1
                    c=0 
                    d=0
                    e=0
                    #print "vmin3 <= vmin4\n"
                end
            end
        else
            n = n - c*10
            d = n / 5     
                
            nmin5 = a*60 + b*20 + (c+1)*10
            vmin5 = a*a_v + b*b_v + (c+1)*c_v 
                
            nmin6 = a*60 + b*20 + c*10 + d*5
            vmin6 = a*a_v + b*b_v + c*c_v + d*d_v    
                
            if n % 5 == 0
                if vmin1 <= vmin5
                    if vmin1 <= vmin6
                        a = a+1
                        b=0
                        c=0
                        d=0
                        e=0
                        #print "vmin1 <= vmin6\n"
                    end
                else
                    if vmin3 <= vmin6
                        b=b+1
                        c=0
                        d=0
                        e=0
                        #print "vmin3 <= vmin6\n"
                    else
                        if vmin5 <= vmin6
                            c=c+1
                            d=0
                            e=0
                            #print "vmin5 <= vmin6\n"
                        end
                    end
                end
            else
                e = n - d*5  
                #print "n=#{n} #{a} #{b} #{c} #{d} #{e}\n"

                nmin7 = a*60 + b*20 + c*10 + (d+1)*5
                vmin7 = a*a_v + b*b_v + c*c_v + (d+1)*d_v

                nmin8 = a*60 + b*20 + c*10 + d*5 + e
                vmin8 = a*a_v + b*b_v + c*c_v + d*d_v + e*e_v
                    
                if vmin1 <= vmin7
                    if vmin1 <= vmin8
                        a = a+1
                        b=0
                        c=0
                        d=0
                        e=0
                        #print "vmin1 <= vmin8\n"
                    end
                else
                    if vmin3 <= vmin8
                        b=b+1
                        c=0
                        d=0
                        e=0
                        #print "vmin3 <= vmin8\n"
                    else
                        if vmin5 <= vmin8
                            c=c+1
                            d=0
                            e=0
                            #print "vmin5 <= vmin8\n"
                        else
                            if vmin7 <= vmin8
                                d=d+1
                                e=0 
                                #print "vmin7 <= vmin8\n"
                            end
                        end                    
                    end
                end     
            end                
        end
    end                
end

#print "1 #{nmin1} #{vmin1}\n"
#print "2 #{nmin2} #{vmin2}\n"
#print "3 #{nmin3} #{vmin3}\n"
#print "4 #{nmin4} #{vmin4}\n"
#print "5 #{nmin5} #{vmin5}\n"
#print "6 #{nmin6} #{vmin6}\n"
#print "7 #{nmin7} #{vmin7}\n"
#print "8 #{nmin8} #{vmin8}\n"

print "#{e} #{d} #{c} #{b} #{a}"


# python
n = int(input())
sixty, twenty, ten, five, one = 0, 0, 0, 0, 0  # 440 230 125 70 15
sixty = n // 60
n %= 60
if n > 20 and (n // 20) * 230 + ((n % 20) // 10) * 125 + ((((n % 20) % 10) // 5)) * 70 + ((((n % 20) % 10) % 5)) * 15 >= 440:
    sixty, n = sixty + 1, n - 60
elif n > 20:
    twenty, n = n // 20, n % 20

if n > 10 and (n // 10) * 125 + (((n % 10) // 5) * 70) + (((n % 10) % 5) * 15) >= 230:
    twenty, n = twenty + 1, n - 20
elif n > 10:
    ten, n = n // 10, n % 10
if n > 5 and ((n // 5) * 70) + ((n % 5) * 15) >= 125:
    ten, n = ten + 1, n - 10
elif n > 5:
    five, n = n // 5, n % 5

if n > 0 and n * 15 >= 70:
    five, n = five + 1, n - 5
elif n > 0:
    one = n
print(one, five, ten, twenty, sixty)


# JS
var stdin = process.openStdin();
stdin.addListener("data", function(e) {
    let data = e.toString().split('\n');
    let n = +data[0];
    getResult(n);
});

function getResult(n){
    let qTickets60 = parseInt(n / 60);
    let remTickets1 = (n % 60);
    
    let cost1 = 440 * (qTickets60 + 1);
    let arr1 = [0, 0, 0, 0, qTickets60 + 1];
    
    let qTickets20 = parseInt((n % 60) / 20);
    let remTickets2 = parseInt((n % 60) % 20);
    let cost2 = 440 * qTickets60 + 230 * (qTickets20 + 1);
    let arr2 = [0, 0, 0, qTickets20 + 1, qTickets60];
    
    let qTickets10 = parseInt(((n % 60) % 20) / 10);
    let remTickets3 = parseInt(((n % 60) % 20) % 10);
    let cost3 = 440 * qTickets60 + 230 * qTickets20 + 125 * (qTickets10 + 1);
    let arr3 = [0, 0, qTickets10 + 1, qTickets20, qTickets60];
    
    let qTickets5 = parseInt((((n % 60) % 20) % 10) / 5);
    let remTickets4 = parseInt((((n % 60) % 20) % 10) % 5);
    let cost4 = 440 * qTickets60 + 230 * qTickets20 + 125 * qTickets10 + 70 * (qTickets5 + 1);
    let arr4 = [0, qTickets5 + 1, qTickets10, qTickets20, qTickets60];
    
    let cost5 = 440 * qTickets60 + 230 * qTickets20 + 125 * qTickets10 + 70 * qTickets5 + 15 * remTickets4;
    let arr5 = [remTickets4, qTickets5, qTickets10, qTickets20, qTickets60];
    
    let min = Math.min(cost1, cost2, cost3, cost4, cost5);
    
    if(min == cost1){
        printArr(arr1);
    }else if(min == cost2){
        printArr(arr2);
    }else if(min == cost3){
        printArr(arr3);
    }else if(min == cost4){
        printArr(arr4);
    }else{
        printArr(arr5);
    }  
}

function printArr(arr){
    console.log(arr[0] + ' ' + arr[1] + ' ' + arr[2] + ' ' + arr[3] + ' ' + arr[4]);
}


# C#
using System;

public class MainClass
{
    public static void Main()
    {
  int n = int.Parse(Console.ReadLine());
  int ans_1,ans_5,ans_10,ans_20,ans_60;
  ans_1=ans_5=ans_10=ans_20=ans_60=0;
  int c,c_1,c_10,c_5,c_20,score;
  
  if (n / 60 > 0)
     {
      ans_60 = n / 60;
      n = n % 60;
     }
     
  if (n <= 5)
      {
        if (n*15 < 70)
             ans_1 = n;
        else       
             ans_5 = 1;
      }
      
  else if (5 > n && n <= 10)
       {
           c = n / 5;
           score = n % 5;
        if (70*c+score* 15 < 125)
            {
                 ans_5 += 1;
                 ans_1 += score;
            } 
          else        
          ans_10 += 1;
       }
       
   else if (10 > n && n <= 20)
        {
          c_10 = n / 10;
          c_5 = (n - c_10 * 10) / 5;
          c_1 = n % 5;
           if (125*c_10+70*c_5+15*c_1 < 230)
            {
               ans_10 = c_10;
               ans_5 = c_5; 
               ans_1 = c_1;
            }
             else
               ans_20 += 1;
         }
         
   else 
        {
        c_20 = n / 20;
        c_10 = (n - c_20 * 20) / 10;
        c_5 = (n-c_20*20-c_10*10) / 5;
        c_1 = n % 5;
     if (c_20*230+c_10*125+c_5*70+c_1*15<440)
             {
               ans_20 = c_20;      
           if (125*c_10+70*c_5+15*c_1<230)
               {
                  ans_10 = c_10;                        if (70 * c_5 + 15 * c_1 < 125)
                  {
                    ans_5 = c_5;
                    ans_1 = c_1;
                  }
                else
                   ans_10 += 1;
                }
            else
                ans_20 += 1;
             }
         else
            ans_60 += 1;
       }
  Console.WriteLine($"{ans_1} {ans_5} {ans_10} {ans_20} {ans_60}");
  
}
}


# java
import java.util.Scanner;

public class Main {

    public static void main(String[] args) {
        Scanner in = new Scanner(System.in);
        int a = in.nextInt();
        in.close();
        int x60 = a / 60;
        int x20 = a % 60 / 20;
        int x10 = a % 60 % 20 / 10;
        int x5 = a % 60 % 20 % 10 / 5;
        int x1 = a % 60 % 20 % 10 % 5;
        if ((x1 * 15 + x5 * 70 + x10 * 125 + x20 * 230) >= 440) {
            x60++; x20 = 0; x10 = 0; x5 = 0; x1 = 0;
        }
        if ((x1 * 15 + x5 * 70 + x10 * 125) >= 230) {
            x20++; x10 = 0; x5 = 0; x1 = 0;
        }
        if ((x1 * 15 + x5 * 70) >= 125) {
            x10++; x5 = 0; x1 = 0;
        }
        if (x1 * 15 >= 70) {
            x5++; x1 = 0;
        }
        System.out.println(x1 + " " + x5 + " " + x10 + " " + x20 + " " + x60);
    }
}


# C
#include <stdio.h>

int main() {
    int n;
    scanf("%d", &n);
    
    int b1=0, b5=0, b10=0, b20=0, b60=0;
    int s1=15, s5=70, s10=125, s20=230, s60=440;
    
    b60 = n/60;
    b20 = n%60/20;
    b10 = n%20/10;
    b5 = n%10/5;
    b1 = n%5/1;
    
    if (b1 * s1 + b5 * s5 + b10 * s10 + b20 * s20 >= s60){
        b60++; 
        b1=0; 
        b5=0;
        b10=0;
        b20=0;
    }else{ 
        if (b1 * s1 + b5 * s5 + b10 * s10 >= s20){
            b20++; 
            b1=0; 
            b5=0;
            b10=0;
        }else{ 
            if (b1 * s1 + b5 * s5 >= s10){
                b10++; 
                b1=0; 
                b5=0;
            }
        }
    }
    printf("%d %d %d %d %d", b1, b5, b10, b20, b60);
    return 0;
}


# C++
#include <iostream>
using namespace std;
int main() {
    int n;
    int p1 = 0, p5 = 0, p10 = 0, p20 = 0, p60 = 0;
    cin >> n;

    //любое количество билетов делим на 60 и дальше работаемс остатком от 60
    p60 = n / 60;
    n = n % 60;

    if (n >= 36) {//если остаток- от 36 до 59, выгоднее взять ещё один билет на 60п.
        p60++;
        n = 0;
    }

    //если остаток меньше 36, отсекаем ещё 20 поездок одним билетом и дальше
    // работаем с отстатком
    if (n >= 20) {
        p20 = 1; //.
        n %= 20;
    }

    if(n >= 17 && n <= 20){
     p20++;
        n = 0;
    }
    // остаток уже от 1 до 15 поездок
    if (n >= 10) {
        p10 = 1;
        n %= 10;
    }

    else if (n == 9) {
        p10++;
        n = 0;
    }

    //остаток от 1 до 6 поездок
    if (n >= 5) {
        p5 = n / 5;
        n %= 5;
    }
    p1 = n;


    cout << p1 << " " << p5 << " " << p10 << " " << p20 << " " << p60;

    //вывод стоимости для проверки в своих тестах
    //cout << endl << p1 * 15 + p5 * 70 + p10 * 125 + p20 * 230 + p60 * 440;
    return 0;
}

