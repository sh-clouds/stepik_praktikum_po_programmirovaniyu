Квадратное уравнение

Даны целые числа a, b, c. Найдите все целочисленные решения квадратного уравнения ax2 + bx + c = 0, если таковые существуют.

Входные данные
Даны три целых числа, a не равно 0.

Выходные данные
Выведите два целых числа, если уравнение имеет два корня (выводить в порядке возрастания, через пробел), одно целое число – при наличии одного корня. При отсутствии целых корней ничего выводить не нужно.

Sample Input:
1
0
0

Sample Output:
0

Напишите программу. Тестируется через stdin → stdout
Верно решили 440 учащихся
Из всех попыток 11% верных


# ruby
a,b,c = gets.to_i,gets.to_i,gets.to_i
#a,b,c = 2,4,0

d = b**2 - 4*a*c
if d==0
    x = -b/(2*a)
    if -b%(2*a)==0
        puts x.to_i
    end
elsif d>0
    x1 = (-b - d**0.5)/(2*a)
    x2 = (-b + d**0.5)/(2*a)  
    
    if x1%1==0 && x2%1==0
        xi1 = x1.to_i
        xi2 = x2.to_i
        if xi1<=xi2
            print "#{xi1} #{xi2}"
        else
            print "#{xi2} #{xi1}"  
        end
    end
end  


# python
a, b, c = [int(input()) for i in range(3)]
d = (b**2)-(4*a*c)
if d == 0:
    x = -1*b/(2*a)
    print((int)(x))
elif d>0:
    x1 = (-b - (d**0.5))/(2*a)
    x2 = (-b + (d**0.5))/(2*a)
    if x1.is_integer() and x2.is_integer():
        if (x1<x2):
            print((int)(x1), (int)(x2))
        else:
            print((int)(x2), (int)(x1))

# JS
var stdin = process.openStdin();
stdin.addListener("data", function(e) {
    let data = e.toString().split('\n');
    let a = +data[0];
    let b = +data[1];
    let c = +data[2];
    getResult(a, b, c);
});

function getResult(a, b, c){
    let d = b * b - 4 * a * c;
    let arr = [];
    
    if(d > 0){
        let x1 = (-b + Math.sqrt(d))/(2 * a);
        let x2 = (-b - Math.sqrt(d))/(2 * a);
        
        if(Number.isInteger(x1)){
            arr.push(parseInt(x1));
        }
        
        if(Number.isInteger(x2)){
            arr.push(parseInt(x2));
        }
        
        let newArr = arr.sort(function(a, b){
            return a - b;
        });
        
        
        if(newArr.length == 2){
            console.log(newArr[0] + ' ' + newArr[1]);
        }else if(newArr.length == 1){
            console.log(newArr[0]);
        }else{
            console.log('');
        }
        
    }else if(d == 0){
        let x1 = (-b + Math.sqrt(d))/(2 * a);
        
        if(Number.isInteger(x1)){
            console.log(parseInt(x1));
        }else{
            console.log('');
        }
    }else{
        console.log('');
    }
}


# C#
using System;

public class MainClass
{
    public static void Main()
    {
            double a = double.Parse(Console.ReadLine());
            double b = double.Parse(Console.ReadLine());
            double c = double.Parse(Console.ReadLine());
            double disk = b * b - 4 * a * c;
            if (disk == 0 && Math.Sqrt(disk) % 1 == 0)
            {
                double res = -b / (2 * a);
                if (res == -0)
                {
                    res = 0;
                }
                Console.WriteLine(res);
            }
            else if (disk > 0 && Math.Sqrt(disk) % 1 == 0)
            {
                double koren = Math.Sqrt(disk);
                double x1 = (-b + koren) / (2 * a);
                double x2 = (-b - koren) / (2 * a);
                if (x1 < x2)
                {
                    Console.WriteLine((int)x1 + " " + (int)x2);
                }
                else
                {
                    Console.WriteLine((int)x2 + " " + (int)x1);
                }
            }
    }
}


# java
import java.util.Scanner;

class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        double a = sc.nextDouble();
        double b = sc.nextDouble();
        double c = sc.nextDouble();
        double D = b * b - (4 * a * c);
        double x1 = (-b + Math.sqrt(D)) / (2 * a);
        double x2 = (-b - Math.sqrt(D)) / (2 * a);
        double check1 = x1 - (int) x1;
        double check2 = x2 - (int) x2;
        if (D > 0) {
            if (check1 == 0 && check2 == 0) {
                if ((int) x1 > (int) x2) {
                    System.out.println((int) x2 + " " + (int) x1);
                } else {
                    System.out.println((int) x1 + " " + (int) x2);
                }
            } else if (check1 != 0 && check2 == 0) {
                System.out.println((int) x2);
            } else if (check1 == 0) {
                System.out.println((int) x1);
        }
        } else if (D < 0) {
            System.out.println();
        } else {
            System.out.println((int)x1);
        }
    }
}


# C
#include <stdio.h>
#include <math.h>

int main() {
    int a, b, c; 
    scanf("%d %d %d", &a, &b, &c);
    
    int d = b * b - 4 * a * c;
    if (!d && !(b % (2 * a))) {
        printf("%d", -b / (2 * a));
    } else if (d > 0) {
        int id = (int)sqrt(d);
        if (d != id * id)
            return 0;
        
        int x1 = (-b - id) / (2 * a);
        int x2 = (-b + id) / (2 * a);
        int f1 = a * x1 * x1 + b * x1 + c;
        int f2 = a * x2 * x2 + b * x2 + c;
        
        if (!f1 && !f2) {
            a = x1 < x2 ? x1 : x2;
            b = x1 > x2 ? x1 : x2;
            printf("%d %d", a, b); 
        }
    }
    return 0;
}



# C++
#include <iostream>
#include <string>
#include <cmath>

int main() {
    long a, b, c, d, x, xi1, xi2;
    double x1, x2;
    std::cin >> a >> b >> c;
    std::string output="";
    
    //a = -1;
    //b = 100;
    //c = 7;
    //0 100
    
    //a = -9;
    //b = 37;
    //c = 11;
    //0 4
    
    d = b*b - 4*a*c;
    //std::cout << std::to_string(d) + "\n";
        
    if (d==0)
    {
        x = -b/(2*a);
        if (-b%(2*a)==0)
        {
            output = std::to_string(x);
            std::cout << output + "\n";
        }
    }   
    else if (d>0)
    {
        x1 = (-b - pow(d,0.5))/(2*a);
        x2 = (-b + pow(d,0.5))/(2*a); 
        //std::cout << std::to_string(x1) + "\n";
        //std::cout << std::to_string(x2) + "\n";
        
        if (x1 == (int)x1 && x2 == (int)x2)    
        {
            xi1 = (int)x1;
            xi2 = (int)x2;
            if (xi1<=xi2)
            {
                output = std::to_string(xi1) + " " + std::to_string(xi2);
                std::cout << output + "\n";
            }    
            else
            {    
                output = std::to_string(xi2) + " " + std::to_string(xi1);
                std::cout << output + "\n";
            }    
        }
    }     
    
    //std::cout << output + "\n";
    
    return 0;
}


