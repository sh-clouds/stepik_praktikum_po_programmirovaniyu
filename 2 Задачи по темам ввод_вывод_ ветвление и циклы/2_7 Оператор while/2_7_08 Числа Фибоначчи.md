Числа Фибоначчи

Последовательность Фибоначчи определяется так:
φ0=0,  φ1=1, ..., φn=φn-1+φn-2.
По данному числу n определите n-е число Фибоначчи φn.

Входные данные
Вводится натуральное число n.

Выходные данные
Выведите ответ на задачу.

Sample Input:
6

Sample Output:
8

Напишите программу. Тестируется через stdin → stdout
Верно решили 664 учащихся
Из всех попыток 66% верных


# ruby
n = gets.to_i
x1 = 0
x2 = 1
i = 0
while i<n
    x1, x2 = x2, x2+x1
    i += 1
end    
print x1


# python
n = int(input())
x1=0
x2=1
i=0
while i<n:
    x1,x2=x2,x2+x1
    i+=1
print(x1)


# JS
let n = Number(require('fs').readFileSync(0, 'utf8'))

let [f0, f1] = [0, 1]
while (n > 1) {
    [f0, f1] = [f1, f0+f1]
    n--
}

console.log(n == 0 ? 0 : f1)


# C#
using System;

Console.Write(Fibonacci(int.Parse(Console.ReadLine())));

static int Fibonacci(int x) => x < 2 ? x : x == 2 ? 1 : Fibonacci(x - 1) + Fibonacci(x - 2);


# java
import java.util.Scanner;

class Main {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        int n = scanner.nextInt();
        int prev = 0;
        int curr = 1;
        while (n-- > 1) {
            curr += prev;
            prev = curr - prev;
        }
        System.out.println(curr);
    }
}


# C
#include <stdio.h>
int main() {
    int a=0,b=1,n,i=0,t;
    scanf("%d",&n);
    while (i<n) (t=a, a=b, b+=t, ++i);    
    printf("%d\n",a);
    return 0;
}


# C++
#include <iostream>

int main() {
    int n, x1 = 0, x2 = 1;
    std::cin >> n;
    while (n--) {
        int t = x2;
        x2 += x1;
        x1 = t;
    }
    std::cout << x1 << "\n";
    return 0;
}



