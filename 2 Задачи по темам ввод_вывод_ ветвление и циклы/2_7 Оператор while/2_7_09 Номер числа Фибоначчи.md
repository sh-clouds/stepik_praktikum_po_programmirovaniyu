Номер числа Фибоначчи

Дано натуральное число A > 1. Определите, каким по счету числом Фибоначчи оно является, то есть выведите такое число n, что φn=A. Если А не является числом Фибоначчи, выведите число -1.

Входные данные
Вводится натуральное число.

Выходные данные
Выведите ответ на задачу.

Sample Input:
8

Sample Output:
6

Напишите программу. Тестируется через stdin → stdout
Верно решили 584 учащихся
Из всех попыток 56% верных


 # ruby
a = gets.to_i
x1=0
x2=1
i=1
while x2<a
    x1,x2=x2,x2+x1
    i+=1
end

if x2!=a
    print -1
else
    print i
end  


# python
a = int(input())
x1=0
x2=1
i=1
while x2<a:
    x1,x2=x2,x2+x1
    i+=1
if x2!=a:
    print(-1)
else:
    print(i)


# JS
let f = Number(require('fs').readFileSync(0, 'utf8'))

let n = 1
let [f0, f1] = [0, 1]
while (f1 < f) {
    [f0, f1] = [f1, f0+f1]
    n++
}

console.log(f1 == f ? n : -1)


# C#
using System;

public class MainClass
{
    public static void Main()
    {
    int a = int.Parse(Console.ReadLine());
if (a == 0)
Console.WriteLine(0);
else if (a == 1)
Console.WriteLine(1);
else
{
int f1 = 0;
int f2 = 1;
int n = 1;
int fn = 0;
   while (fn < a)
   {
   fn = f1 + f2;
   f1=f2;
   f2 = fn;
   n++;
   }
   if (fn == a)
     Console.WriteLine(n);
    else
Console.WriteLine(-1);
    }
    }
}


# java
import java.util.Scanner;

class Main {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        int a = scanner.nextInt();
        int prev = 0;
        int curr = 1;
        int n = 1;
        while (curr < a) {
            curr += prev;
            prev = curr - prev;
            n++;
        }
        System.out.println(curr == a ? n : -1);
    }
}


# C
#include <stdio.h>
int main() {
    int a=0,b=1,i=0,t,x;
    scanf("%d",&x);
    while (a<x) (t=a, a=b, b+=t, ++i);
    printf("%d\n",a==x ? i : -1);
    return 0;
}


# C++
#include <iostream>

int main() {
    int a, x1 = 0, x2 = 1, i = 0;
    std::cin >> a;
    while (x1 < a) {
        int t = x2;
        x2 += x1;
        x1 = t;
        i++;
    }
    std::cout << (x1 == a ? i : -1) << "\n";
    return 0;
}    


