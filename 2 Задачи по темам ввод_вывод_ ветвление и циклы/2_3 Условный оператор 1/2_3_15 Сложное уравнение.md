Сложное уравнение

Решить в целых числах уравнение ( ax + b ) : ( cx + d ) = 0.

Входные данные
Вводятся 4 числа: a, b, c и d; c и d не равны нулю одновременно.

Выходные данные
Необходимо вывести все целочисленные решения, если их число конечно, “NO” (без кавычек), если целочисленных решений нет, и “INF” (без кавычек), если их бесконечно много.

Sample Input:
1
1
2
2

Sample Output:
NO

Напишите программу. Тестируется через stdin → stdout
Верно решили 511 учащихся
Из всех попыток 26% верных

# ruby code here
a, b, c, d = gets.to_i, gets.to_i, gets.to_i, gets.to_i
# ( ax + b ) : ( cx + d ) = 0
#ax + b = 0 * (cx + d)  
#a, b = 0, 1
#x = -b/a
#c = 0: 
puts a==0 && b!=0 ? 'NO' : a==0 ? 'INF' : b!=0 && -b%a!=0 ? 'NO' : (-b/a) * c + d == 0 ? 'NO' : -b/a 


# python code here
def solve_equation(a, b, c, d):
    if a == 0:
        if b == 0:
            print("INF")
        else:
            print("NO")
    else:
        x = -b / a
        if c * x + d != 0:
            if x == int(x):
                print(int(x))
            else:
                print("NO")
        else:
            print("NO")


a = int(input())
b = int(input())
c = int(input())
d = int(input())

solve_equation(a, b, c, d)


# JS
process.stdin.setEncoding('utf8');
process.stdin.on('readable', () => main(process.stdin.read()));

const main = (args) => {
  if (!args) return;
  const [a, b, c, d] = processArgs(args);
  let result = 'NO';
  if (a === 0 && b === 0) {
    result = 'INF';
  } else if (b % a === 0) {
    const x = getResult(a, b);
    result = x === -d / c ? 'NO' : x;
  }
  console.log(result);
};

const getResult = (a, b) => (b === 0 ? 0 : -b / a);

const processArgs = (args) =>
  args.split('\n').map((i) => parseInt(i.trim(), 10));


# C#
using System;

public class MainClass
{
    public static void Main()
    {
     int a=int.Parse(Console.ReadLine());
     int b=int.Parse(Console.ReadLine());
     int c=int.Parse(Console.ReadLine());
     int d = int.Parse(Console.ReadLine());
     if (a == 0 && b == 0)
     Console.WriteLine("INF");
     else if (a == 0 || b * c == a * d)
      Console.WriteLine("NO");
    else if (b % a == 0)
     {int x = -b / a;
     Console.WriteLine(x);}
     else
    Console.WriteLine("NO");
    }
}


# java
import java.util.Scanner;

class Main{
    public static void main(String[] args){
        Scanner scan = new Scanner(System.in);
        int a = scan.nextInt(), b = scan.nextInt();
        int c = scan.nextInt(), d = scan.nextInt();
        scan.close();
        if (a != 0 && c != 0 && b / a == d / c) {
            System.out.println("NO");
        }else{
        if (a == 0 && b == 0)
             System.out.println("INF");
        if (a == 0 && b != 0)
             System.out.println("NO");
        if (a != 0 && b % a != 0)
             System.out.println("NO");
        if (a != 0 && b % a == 0)
             System.out.println((b / a) * (-1));}
        

    }
}


# C
#include <stdio.h>

int main() {
    int a, b, c, d;
    scanf("%d %d %d %d", &a, &b, &c, &d);
    
    if (a == 0) {
        puts(b == 0 ? "INF" : "NO");
        return 0;
    }
    int x = -b / a;
    if (a * x + b == 0 && c * x + d)
        printf("%d", x);
    else
        puts("NO");
    return 0;
}


# C++
#include <iostream>
#include <string>

int main() {
    int a, b, c, d;
    std::cin >> a >> b >> c >> d;
    std::string output;
    
    if ((a == 0) && (b == 0))
    {
        output = "INF\n";
    }
    else if ((a == 0) || ((a*d) == c*b))
    {
        output = "NO\n";
    }
    else if ((b % a) == 0)
    {
        output = std::to_string(-b/a);
    }
    else
    {
        output = "NO\n";
    }
    
    std::cout << output;
    
    return 0;
}



