Квадраты

Входные данные
Вводятся целые числа a и b. Гарантируется, что a не превосходит b.

Выходные данные
Выведите через пробел все числа на отрезке от a до b включительно, являющиеся полными квадратами. Если таких чисел нет, то ничего выводить не нужно.

Sample Input:
2
8

Sample Output:
4

Напишите программу. Тестируется через stdin → stdout
Верно решили 668 учащихся
Из всех попыток 28% верных

# ruby
a,b = gets.to_i, gets.to_i
m = []


a.upto(b) do |i|
    c = i.pow(0.5)
    
    if i && i==c.to_i.pow(2)
        m.push(i)
    end
end    
    
puts m.join(' ') 


# python
a, b = int(input()), int(input())

for i in range(a, b + 1):
    if i % i**0.5 == 0:
        print(i, end = ' ')


# JS
var stdin = process.openStdin();

stdin.addListener("data", function(d) {
    let [a, b] = d.toString().split('\n');
    a = +a;
    b = +b;
    let result = '';
    
    for (let i=a; i<=b; i++) {
        let str = (i**0.5).toString();
        if (!str.includes('.')) result += i + ' ';
    }

    console.log(result);
  });


# C#
using System;
using static System.Console;


public class MainClass
{
    public static void Main()
    {
        int a = Int32.Parse(ReadLine());
        int b = Int32.Parse(ReadLine());
        
        int i = (int)Math.Ceiling(Math.Sqrt(a));
        for (; i * i <= b; i++)
            Write($"{i * i} ");
    }
}


# java
import java.util.Scanner;

class Main {
    public static void main(String[] args) {
        Scanner scan=new Scanner(System.in);
        int a=scan.nextInt();
        int b=scan.nextInt();
        for(int i=a;i<(b+1);i++){
            if (((Math.sqrt(i))*10)%10==0){
                System.out.print(i);
                System.out.print(' ');
            }
        }
    }
}


# C
#include <stdio.h>
#include <math.h>
int main() {
    for (int a,b; scanf("%d%d",&a,&b)==2; puts(""))
        for (int i=pow(a,0.5), q=(q=i*i, q<a ? ++i : 0); (q=i*i, q<b+1); printf("%d ",q), ++i); 
    return 0;
}


# C++
#include <iostream>

bool is_square(int n) {
    for (int i = 1; i < n; i++) {
        if (i * i == n) return true;
    }
    return false;
}

int main() {
    int a, b;
    std::cin >> a >> b;
    for (int i = a; i <= b; i++) {
        if (is_square(i)) std::cout << i << " ";
    }
    return 0;
}




