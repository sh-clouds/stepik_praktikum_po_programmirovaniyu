Делители числа

Выведите все натуральные делители числа x в порядке возрастания (включая 1 и само число).

Входные данные
Вводится натуральное число x

Выходные данные
Выведите все делители числа x

Sample Input:
32

Sample Output:
1 2 4 8 16 32

Напишите программу. Тестируется через stdin → stdout
Верно решили 795 учащихся
Из всех попыток 67% верных


# ruby
x = gets.to_i
m=[]

1.upto(x) do |i|  
    if x % i==0 
        m.push(i)
    end
end

print m.join(' ')


# python
n = int(input())
for i in range(1,n+1):
  if n%i==0:
    print(i,end=" ")


# JS
var stdin = process.openStdin();

stdin.addListener("data", function(d) {
    let n = Math.abs(+d);
    let result = 1 + ' ' ;
    
    for (let i=2; i<=n/2; i++) 
        if (n%i === 0) result = result + i + ' ';
    result += n;
    
    if (n === 1) result = 1;

    console.log(result);
  });
  

# C#
using System;

public class MainClass
{
    public static void Main()
    {
        int n = Convert.ToInt32(Console.ReadLine());
        for (int i = 1;i <= n;i++)
        {
            if (n % i == 0)
            Console.Write($"{i} ");
        }
    }
}



# java
import java.util.Scanner;
class Main {
    public static void main(String[] args) {
        Scanner scan=new Scanner(System.in);
        int x=scan.nextInt();
        for(int i=1;i<=x;i++){
            if (x%i==0){
                System.out.print(i);
                System.out.print(' ');
            }
        }
    }
}


# C
#include <stdio.h>
int main() {
    for (int x; scanf("%d",&x)==1; puts(""))
        for (int a=1; a<x+1; x%a ? 0 : printf("%d ",a), ++a);
    return 0;
}


# C++
#include <iostream>

int main() {
    int x;
    std::cin >> x;
    for (int i = 1; i <= x; i++) {
        if (x % i == 0) {
            std::cout << i << " ";
        }
    }
    return 0;
}


# rust
use std::io::{self, Read};

fn main() {
    // Read the whole input and parse it as an unsigned integer
    let mut input = String::new();
    io::stdin().read_to_string(&mut input).expect("Failed to read stdin");
    let limit: usize = input.trim().parse().expect("Input must be an integer");

    // Collect all divisors of `limit`
    let mut divisors: Vec<usize> = Vec::new();
    for candidate in 1..=limit {
        if limit % candidate == 0 {
            divisors.push(candidate);
        }
    }

    // Convert each divisor to a string and join them with spaces
    let output = divisors
        .iter()
        .map(|d| d.to_string())
        .collect::<Vec<String>>()
        .join(" ");

    println!("{}", output);
}






