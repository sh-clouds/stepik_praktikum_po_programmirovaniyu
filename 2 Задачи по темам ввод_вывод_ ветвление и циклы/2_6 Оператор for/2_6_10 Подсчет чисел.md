Подсчет чисел

Подсчитайте, сколько среди данных N чисел нулей, положительных чисел, отрицательных чисел.

Входные данные
Вводится число N, а затем N целых чисел.

Выходные данные
Необходимо вывести сначала число нулей, затем число положительных и отрицательных чисел.

Sample Input:
5
28
0
0
0
0

Sample Output:
4 1 0

Напишите программу. Тестируется через stdin → stdout
Верно решили 780 учащихся
Из всех попыток 69% верных


# ruby
n = gets.to_i
cnt_0, cnt_p, cnt_n = 0,0,0

1.upto(n) do |i| 
    a = gets.to_i
    if a==0
        cnt_0 += 1
    elsif a>0
        cnt_p += 1
    elsif a<0
        cnt_n += 1        
    end
end

puts "#{cnt_0} #{cnt_p} #{cnt_n}"


# python
n, a = int(input()), [] 
while len(a) < n:
    a.append(int(input()))
print(a.count(0), len(list(filter(lambda x: x>0, a))), len(list(filter(lambda x: x<0, a))))


# JS
var stdin = process.openStdin();

stdin.addListener("data", function(d) {
    let data = d.toString().split('\n');
    let [zero, plus, minus] = [0, 0, 0];

    for (let i=1; i<data.length; i++) 
        if ( +data[i] === 0 ) zero++
        else if ( +data[i] > 0 ) plus++
        else minus++;
    
    console.log(zero + ' ' + plus + ' ' + minus);
  });


# C#
using System;

var n = int.Parse(Console.ReadLine());
var z = 0;
var p = 0;
var m = 0;
for (int i = 0; i < n; i++)
{
    var temp = int.Parse(Console.ReadLine());
    if (temp > 0) p++;
    else if (temp < 0) m++;
    else z++;
}
Console.Write($"{z} {p} {m}");


# java
import java.util.Scanner;
class Main {
    public static void main(String[] args) {
        Scanner scan=new Scanner(System.in);
        int n;
        n=scan.nextInt();
        int x;
        int kol1 = 0;
        int kol2 = 0;
        int kol3 = 0;
        for(int i=0;i<n;i++){
            x=scan.nextInt();
            if (x==0){
                kol1++;
            }else if (x>0){
                kol2++;
            }else{
                kol3++;
            }
        }
        System.out.printf("%d %d %d",kol1,kol2,kol3);
    }
}


# C
#include <stdio.h>
int main() {
    for (int d,z,p,n=p=z=0; scanf("%d",&d)==1; printf("%d %d %d\n",z,p,n))
        for (int i=0,x; i<d; ++i, scanf("%d",&x), x==0 ? ++z : x>0 ? ++p : ++n);
    return 0;
}


# C++
#include <iostream>

int main() {
    int n, C_0 = 0, C_p = 0, C_o = 0;
    std::cin >> n;
    for (int i = 0, end = n; i < end; i++) {
        std::cin >> n;
        if (n > 0) {
            C_p++;
        } else if (n < 0) {
            C_o++;
        } else {
            C_0++;
        }
    }
    std::cout << C_0 << " " << C_p << " " << C_o << "\n";
    return 0;
}


