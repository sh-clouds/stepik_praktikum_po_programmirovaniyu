Ноль или не ноль

Проверьте, есть ли среди данных N чисел нули.

Входные данные
Вводится число N, а затем N чисел.

Выходные данные
Выведите YES, если среди введенных чисел есть хотя бы один нуль, или NO в противном случае.

Sample Input:
3
1
0
2

Sample Output:
YES

Напишите программу. Тестируется через stdin → stdout
Верно решили 777 учащихся
Из всех попыток 62% верных



# ruby
n = gets.to_i
cnt_0 = 0

1.upto(n) do |i| 
    a = gets.to_i
    if a==0
        cnt_0 += 1      
    end
end

puts  cnt_0>0 ? 'YES': 'NO' 


# python
s = [int(input()) for _ in range(int(input()))]
print('YES' if 0 in s else 'NO')


# JS
var stdin = process.openStdin();

stdin.addListener("data", function(d) {
    let data = d.toString().split('\n');
    let result = 'NO';

    for (let i=1; i<data.length; i++) 
        if ( +data[i] === 0 ) { 
            result = 'YES';
            break;
        }
    
    console.log(result);
  });


# C#
using System;

var n = int.Parse(Console.ReadLine());
var res = "NO";
for (int i = 0; i < n; i++)
    if (Console.ReadLine() == "0")
    {
        res = "YES";
        break;
    }
Console.Write(res);


# java
import java.util.Scanner;
class Main {
    public static void main(String[] args) {
        Scanner scan=new Scanner(System.in);
        int n;
        n=scan.nextInt();
        int x;
        for(int i=0;i<n;i++){
            x=scan.nextInt();
            if (x==0) {
                System.out.print("YES");
                return;
            }
        }
        System.out.print("NO");
    }
}


# C
#include <stdio.h>
int main() {
    for (int n,z=0; scanf("%d",&n)==1; puts(z ? "YES" : "NO"))
        for (int i=0,x; i<n; ++i, scanf("%d",&x), x==0 ? ++z : 0);
    return 0;
}


# C++
#include <iostream>

int main() {
    int n, val;
    std::cin >> n;
    bool wasZero = false;
    for (int i = 0; i<n; i++) {
        std::cin >> val;
        if (!val) {
            wasZero = true;
            break;
        }
    }
    std::cout << (wasZero?"YES":"NO");
    return 0;
}


