Количество решений

Входные данные
Вводятся 5 чисел: a, b, c, d и e.

Выходные данные
Найдите все целые решения уравнения ( ax3 + bx2 + cx + d ) / ( x - e ) = 0 на отрезке [0,1000] и выведите их количество.

Sample Input:
1
2
3
4
5

Sample Output:
0

Напишите программу. Тестируется через stdin → stdout
Верно решили 539 учащихся
Из всех попыток 43% верных


# ruby
a,b,c,d,e = gets.to_i, gets.to_i, gets.to_i, gets.to_i, gets.to_i
m=[]

0.upto(1000) do |i| 
    if (a*i.pow(3) + b*i.pow(2) + c*i + d ==0) && (i != e)
        m.push(i)      
    end
end

puts  m.size


# python
a, b, c, d, e = (int(input()) for _ in range(5))
res = 0
for x in range(0, 1000):
    if x - e != 0:
        if (a*x**3 + b*x**2 + c*x + d)/(x - e) == 0:
            res+=1
print(res)


# JS
let [a, b, c, d, e] = require('fs').readFileSync(0, 'utf8').split('\n').map(Number);

let res = 0;
for (let x = 0; x <= 1000; x++) {
  if ((a*x**3 + b*x**2 + c*x + d) / (x-e) == 0)
    res++;
} 

console.log(res);


# C#
using System;
using System.Collections.Generic;

var a = int.Parse(Console.ReadLine());
var b = int.Parse(Console.ReadLine());
var c = int.Parse(Console.ReadLine());
var d = int.Parse(Console.ReadLine());
var e = int.Parse(Console.ReadLine());
var res = 0;
for (int i = 0; i <= 1000; i++)
    if ((a * Math.Pow(i, 3) + b * Math.Pow(i, 2) + c * i + d) / (i - e) == 0) res++;
Console.Write(res);


# java
import java.util.Scanner;
import java.util.stream.IntStream;

class Main {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        int a = scanner.nextInt();
        int b = scanner.nextInt();
        int c = scanner.nextInt();
        int d = scanner.nextInt();
        int e = scanner.nextInt();
        long count = IntStream.rangeClosed(0, 10000)
                .filter(i -> i != e && a * i * i * i + b * i * i + c * i + d == 0)
                .count();
        System.out.println(count);
    }
}


# C
#include <stdio.h>

int main() {
    int a,b,c,d,e, count = 0;
    scanf("%d%d%d%d%d",&a,&b,&c,&d,&e);
    for(double i=0;i<=1000;i++)
        if(i-e!=0 && (a*i*i*i+b*i*i+c*i+d)/(i-e)==0 && i==(int)i) count++;
    printf("%d",count);
         
    // put your code here
    return 0;
}


# C++
#include <iostream>

int main() {
    float a, b, c, d, e;
    std::cin >> a >> b >> c >> d >> e;
    int rootNum = 0;
    for (int x=0; x<=1000; x++) {
        if ( (x - e) == 0 ) continue;
        else if ( (a*x*x*x + b*x*x + c*x + d) == 0 ) rootNum++;
    }
    std::cout << rootNum;
    return 0;
}



