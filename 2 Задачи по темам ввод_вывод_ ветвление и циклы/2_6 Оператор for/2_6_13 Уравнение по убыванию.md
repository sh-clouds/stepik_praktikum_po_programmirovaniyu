Уравнение по убыванию

Входные данные
Вводятся 4 числа: a, b, c и d.

Выходные данные
Найдите все целые решения уравнения ax3 + bx2 + cx + d = 0 на отрезке [0,1000] и выведите их в порядке убывания. Если на данном отрезке нет ни одного решения, то ничего выводить не нужно.

Sample Input:
1
-5
6
0

Sample Output:
3 2 0

Напишите программу. Тестируется через stdin → stdout
Верно решил 581 учащийся
Из всех попыток 74% верных


# put your ruby code here
a,b,c,d = gets.to_i, gets.to_i, gets.to_i, gets.to_i
m=[]

0.upto(1000) do |i| 
    if a*i.pow(3) + b*i.pow(2) + c*i + d ==0
        m.push(i)      
    end
end

if m.size>0
    puts  m.sort.reverse.join(' ')
end 


# put your python code here
def f(x: int) -> int:
    return a * x ** 3 + b * x ** 2 + c * x + d

a, b, c, d = [int(input()) for _ in range(4)]
print(*[i for i in range(1001) if f(i) == 0][::-1])


# JS
let [a, b, c, d] = require('fs').readFileSync(0, 'utf8').split('\n').map(Number);

let res = [];
for (let x = 1000; x >= 0; x--) {
  if (a*x**3 + b*x**2 + c*x + d == 0)
    res.push(x);
} 

console.log(res.join(' '));



# C#
using System;
using System.Collections.Generic;

var a = int.Parse(Console.ReadLine());
var b = int.Parse(Console.ReadLine());
var c = int.Parse(Console.ReadLine());
var d = int.Parse(Console.ReadLine());
var res = new List<double>();
for (int i = 0; i <= 1000; i++)
    if (a * Math.Pow(i, 3) + b * Math.Pow(i, 2) + c * i + d == 0) res.Add(i);
res.Reverse();
Console.Write(res.Count > 0 ? String.Join(" ", res) : "");


# java
import java.util.Scanner;

class Main {
    public static void main(String[] args) {
        Scanner scan=new Scanner(System.in);
        int a=scan.nextInt();
        int b=scan.nextInt();
        int c=scan.nextInt();
        int d=scan.nextInt();
        for(int i=1000;i>=0;i--){
            if ((a*Math.pow(i, 3))+(b*Math.pow(i, 2))+(c*i)+d==0){
                System.out.print(i);
                System.out.print(' ');
            }
        }
    }
}


# C
#include <stdio.h>

int main() {
    // put your code here
    int a = 0, b = 0, c = 0, d = 0;
    scanf("%d %d %d %d", &a, &b, &c, &d);
    
    for(int i = 1000; i >= 0; --i){
        int x2 = i * i;
        int x3 = x2 * i;
        if(a * x3 + b * x2 + c * i + d == 0){
            printf("%d ", i);
        }
    }    
    return 0;
}

# C++
#include <iostream>

int main() {
    int a, b, c, d;
    std::cin >> a >> b >> c >> d;
    for (int x=1000; x>=0; x--) {
        if(!(a*x*x*x+b*x*x+c*x+d)) std::cout << x << ' ';
    }
    return 0;
}


