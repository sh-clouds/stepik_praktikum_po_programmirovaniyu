Минимальная и максимальная цифры

Входные данные
Задано единственное число N, не больше 1000.

Выходные данные
Необходимо вывести наименьшую и наибольшую цифры данного числа через пробел.

Sample Input:
37

Sample Output:
3 7

Напишите программу. Тестируется через stdin → stdout
Верно решили 739 учащихся
Из всех попыток 69% верных


# ruby
a = gets.to_s.split('').map { |j| j.to_i }.sort
print ("#{a[0]} #{a[-1]}")


# python
print(min((lst := list(map(int, input())))), max(lst))


# JS
process.stdin.on('readable', () => {
    let data = process.stdin.read();
    if (data) {
        const [...N] = data.toString();
        console.log(Math.min(...N), Math.max(...N));
    }
});


# C#
using System;
using System.Collections.Generic;
using System.Linq;

public class MainClass
{
    public static void Main()
    {
        int x=int.Parse(Console.ReadLine());
              List <int>num=new List <int>();
            while (x>0)
                {
                    num.Add(x%10);
                x/=10;
               }
        Console.WriteLine($"{num.Min()} {num.Max()}");
    }
}


# java
import java.util.Arrays;
import java.util.IntSummaryStatistics;
import java.util.Scanner;

class Main {
    public static void main(String[] args) {
        try (Scanner scanner = new Scanner(System.in)) {
            IntSummaryStatistics statistics = Arrays.stream(scanner.nextLine().split(""))
                                                    .mapToInt(Integer::parseInt)
                                                    .summaryStatistics();

            System.out.println(statistics.getMin() + " " + statistics.getMax());
        }
    }
}


# C
#include <stdio.h>

int main() {
    int n;
    int min = 9;
    int max = 0;
    int tmp;
    scanf("%d", &n);
    while (n > 0) {
        tmp = n % 10;
        if (tmp < min){
           min = tmp; 
        } 
        if (tmp > max) {
            max = tmp;
          }
        n /=10;
    }
    printf ("%d %d", min, max);
    return 0;
}

# C++
#include <iostream>

int main() {
    int n, min = -1, max = 0;
    std::cin >> n;
    while (n) {
        if (n % 10 > max) max = n % 10;
        if (min == -1 || n % 10 < min) min = n % 10;
        n /= 10;
    }
    std::cout << min << " " << max << "\n";
    return 0;
}


