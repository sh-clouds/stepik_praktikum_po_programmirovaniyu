Количество нулей

Входные данные
Задано единственное число N, не больше 1000.

Выходные данные
Необходимо вывести количество нулей среди всех цифр числа N.

Sample Input:
100

Sample Output:
2

Напишите программу. Тестируется через stdin → stdout
Верно решили 746 учащихся
Из всех попыток 77% верных


# ruby
puts gets.to_s.split('').map { |j| j.to_i==0 ? j=1 : j=0}.sum


# python
print(input().count('0'))


# JS
let n = require('fs').readFileSync(0, 'utf8').split('').map(Number);
let result = n.filter(el => el === 0).length;
console.log(result);


# C#
using System;

public class MainClass
{
    public static void Main()
    {
        string n = Console.ReadLine();
        int amountOfZero = 0;
        for(int i = 0; i < n.Length; ++i)
        {
            if (n[i] == '0')
            ++amountOfZero;
        }
        Console.WriteLine(amountOfZero);
    }
}


# java
import java.util.Scanner;

class Main {
    public static void main(String[] args) {
        try (Scanner scanner = new Scanner(System.in)) {
            System.out.println(scanner.useDelimiter("")
                                      .tokens()
                                      .mapToInt(Integer::parseInt)
                                      .filter(value -> value == 0)
                                      .count());
        }
    }
}


# C
#include <stdio.h>

int main() {
    int n;
    int x = 0;
    int count = 0;
    scanf("%d", &n);
    while(n > 0){
        x = n % 10;
        n /= 10;
        if (x == 0) {
            count++;
        }
    }
    printf("%d", count);
    return 0;
}


# C++
#include <iostream>

int main() {
    int n;
    std::cin >> n;
    int numZeros = 0;
    while (n) {
        if (!(n%10)) numZeros++;
        n /= 10;
    }
    std::cout << numZeros;
    return 0;
}


