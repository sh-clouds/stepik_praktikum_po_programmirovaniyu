Сумма цифр числа

Входные данные
Задано единственное число N (0 < N <= 1000).

Выходные данные
Необходимо вывести сумму цифр числа N.

Sample Input:
42

Sample Output:
6

Напишите программу. Тестируется через stdin → stdout
Верно решили 554 учащихся
Из всех попыток 74% верных


# ruby
puts gets.to_s.split('').map { |j| j.to_i }.sum

# python
print(sum(int(s) for s in input()))


# JS
process.stdin.on('readable', () => {
    let data = process.stdin.read();
    if (data) {
        N = [...data.toString()];
        console.log(N.reduce((a, b)=> +a + +b));
    }
});


# C#
using System;
using static System.Console;

public class MainClass
{
    public static void Main()
    {
        int a = int.Parse(ReadLine());
        int s = 0;
        while (Convert.ToBoolean(a))
        {
            s = s + a % 10;
            a = a / 10;
        }
        Console.Write(s);
    }
}


# java
import java.util.Scanner;

class Main {
    public static void main(String[] args) {
        try (Scanner scanner = new Scanner(System.in)) {
            System.out.println(scanner.useDelimiter("")
                                      .tokens()
                                      .mapToInt(Integer::parseInt)
                                      .sum());
        }
    }
}


# C
#include <stdio.h>
int main() {
    for (int x,s=(scanf("%d",&x),0); x || (printf("%d",s),0); s+=x%10, x/=10);
    return 0;
}


# C++
#include <iostream>

int main() {
    int n, s = 0;
    std::cin >> n;
    while (n) {
        s += n % 10;
        n /= 10;
    }
    std::cout << s << "\n";
    return 0;
}


