НОД

Даны два числа. Найти их наибольший общий делитель.

Входные данные
Вводятся два натуральных числа.

Выходные данные
Выведите НОД введенных чисел.

Sample Input 1:
9 12

Sample Output 1:
3

Sample Input 2:
928456982736495876239876592387469578236500 216498237659872365987387562938749785625

Sample Output 2:
125

Напишите программу. Тестируется через stdin → stdout
Верно решили 145 учащихся
Из всех попыток 59% верных

# ruby
def nod(a,b)
    while a != 0 and b != 0 do
        if a > b
            a = a % b
        else
            b = b % a
        end
    end
    return a+b
end  

a,b  = gets.to_i, gets.to_i 
puts nod(a,b)


# ruby 2
n1, d1, n2, d2 = gets.split().map(&:to_i)
ratio_1 = Rational(n1, d1)
ratio_2 = Rational(n2, d2)
ratio = ratio_1 + ratio_2
numerator = ratio.numerator
denominator = ratio.denominator
print(numerator, ' ', denominator)


# python
a, b = map(int, input().split())
print(__import__('math').gcd(a, b))


# JS
let [m, n] = require('fs').readFileSync(0, 'utf8').split(' ').map(BigInt);

while (m != 0 && n != 0) {
    if (m > n)
        m = m % n;
    else
        n = n % m;
}

console.log(Number(m + n));


# java
import java.util.Scanner;
import java.math.BigInteger;
class Main {
    public static void main(String[] args) {
Scanner v = new Scanner(System.in);
BigInteger a=new BigInteger(v.next());
BigInteger b=new BigInteger(v.next());        
BigInteger ost1=BigInteger.valueOf(1);
BigInteger ost2=BigInteger.valueOf(1); 
if(a.compareTo(b)==0){System.out.println(a);}
else if(b.compareTo(a)>0){BigInteger c=a;
                          a=b;b=c;}      

for(int i=0;;i++){
    if(i==0){
        ost1=a.mod(b);
        if(ost1.compareTo(BigInteger.valueOf(0))==0){System.out.println(b);break;}
    }
    else{ost2=b.mod(ost1);
        if(ost2.compareTo(BigInteger.valueOf(0))==0){System.out.println(ost1);break;}
        b=ost1;
        ost1=ost2;
        }
}
     
    }
}