Шестеренки

Даны две сцепленные шестеренки. У одной шестеренки N зубцов, у другой – K. Требуется найти, какое минимальное число поворотов на один зубчик требуется сделать, чтобы шестеренки вернулись в исходное состояние.

Входные данные
В единственной строке – два натуральных числа N и K, не превосходящих 10 миллионов.

Выходные данные
Выведите искомое количество зубчиков.

Входные данные 1
2 3

Выходные данные 1
6


Входные данные 2
6 21

Выходные данные 2
42


Напишите программу. Тестируется через stdin → stdout
 Отлично!
Теперь вам доступен Форум решений , где вы можете сравнить свое решение с другими или спросить совета.


Верно решили 90 учащихся
Из всех попыток 61% верных



# ruby
n, k  = gets.chomp.split.map(&:to_i)
p n/(n.gcd(k))*k



# python
from math import gcd 

n, k = map(int, input().split())
print(n * k // gcd(n, k))



# java
import java.util.Scanner;
class Main {
    public static void main(String[] args) {
    Scanner myscan=new Scanner(System.in);
        long a=myscan.nextLong(),b=myscan.nextLong();
        long i=a,j=b;
        while(i!=0){long t=i;i=j%i;j=t;}
        System.out.println((a/j)*b);
    }
}



# rust
use std::io::{self, Read};

/// Computes the greatest common divisor of two integers using the Euclidean algorithm.
fn gcd(mut a: i64, mut b: i64) -> i64 {
    while b != 0 {
        let remainder = a % b;
        a = b;
        b = remainder;
    }
    a.abs()
}

fn main() {
    // Read the entire input from stdin.
    let mut input = String::new();
    io::stdin()
        .read_to_string(&mut input)
        .expect("Failed to read input");

    // Parse the two integers.
    let mut numbers = input.split_whitespace().map(|s| s.parse::<i64>().unwrap());
    let n = numbers.next().expect("Missing first integer");
    let k = numbers.next().expect("Missing second integer");

    // Compute the least common multiple: lcm = n * k / gcd(n, k)
    let lcm = n * k / gcd(n, k);

    println!("{}", lcm);
}






