Сложить две дроби

Даны две рациональные дроби: a/b и c/d. Сложите их и результат представьте в виде несократимой дроби m/n.

Входные данные
Программа получает на вход 4 натуральных числа a, b, c, d, не превосходящих 100.

Выходные данные
Программа должна вывести 2 натуральных числа m и n такие, что m/n=a/b+c/d и дробь m/n – несократима.

Sample Input:
1 3 1 2

Sample Output:
5 6

Напишите программу. Тестируется через stdin → stdout
Верно решили 186 учащихся
Из всех попыток 44% верных


# ruby
a,b,c,d = gets.chomp.split().map{|j| j.to_i}
#a/b + c/d = m/n
#a*d + b*c / b* d
m = a*d + b*c
n = b*d

m_a, n_a, a = [], [], []

1.upto(m) do |i|  
    if m % i==0 
        m_a.push(i)
    end
end

1.upto(n) do |i|  
    if n % i==0 
        n_a.push(i)
    end
end

n_a.each do |i| 
    if m_a.include?(i)
        a.push(i) 
    end 
end 

print "#{m/a[-1]} #{n/a[-1]}" 


# ruby 2
n1, d1, n2, d2 = gets.split().map(&:to_i)
ratio_1 = Rational(n1, d1)
ratio_2 = Rational(n2, d2)
ratio = ratio_1 + ratio_2
numerator = ratio.numerator
denominator = ratio.denominator
print(numerator, ' ', denominator)


# python
a, b, c, d = map(int, input().split())
m, n = a*d + b*c, b*d
nd = (nod := lambda a, b: b and nod(b, a%b) or a)(m,n)
print(m//nd, n//nd, sep='


# JS
const nod = (x, y) => {
    if (y > x)
        return nod(y, x);
    if (!y)
        return x;
    return nod(y, x % y);
}

let [a, b, c, d] = require('fs').readFileSync(0, 'utf8').split(' ').map(Number);
let [m, n] = [a*d + c*b, b*d];
let nd = nod(m, n);

console.log(m/nd, n/nd);


# C#
using System;

public class MainClass
{
    public static void Main()
    {
        int a, b, c, d, m, n, gcd;
        
        var data = Console.ReadLine().Split();
        a = Convert.ToInt32(data[0]);
        b = Convert.ToInt32(data[1]);
        c = Convert.ToInt32(data[2]);
        d = Convert.ToInt32(data[3]);
        
        m = a*d + c*b;
        n = b*d;
        gcd = GCD(m, n);
        
        Console.WriteLine($"{m/gcd} {n/gcd}");
    }
    
    static int GCD(int a, int b)
    {
        return b == 0 ? a : GCD(b, a % b);
    }
}


# java
import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
        Scanner input = new Scanner(System.in);
        int a = input.nextInt(), b = input.nextInt(), c = input.nextInt(), d = input.nextInt();
        int numerator, denominator;

        if(b >= d) denominator = b % d != 0 ? b * d : b;
        else denominator = d % b != 0 ? b * d : d;

        if(denominator == b) numerator = a + c * (b / d);
        else if (denominator == d) numerator = a * (d / b) + c;
        else numerator = a * d + c * b;

        for (int index = 2; index <= denominator ; index++) {
            if(numerator % index == 0 && denominator % index == 0) {
                numerator /= index;
                denominator /= index;
                index--;
            }
        }

        System.out.printf("%d %d", numerator, denominator);
    }
}



# C++
#include <iostream>

using namespace std;

int main()
{
    int a, b, c, d, m, n, nod = 1, x;
    cin >> a >> b >> c >> d;
    n = b * d;
    m = a * d + c * b;
    if(m < n)
        x = m;
    else
        x = n;
    for(int i = 1; i <= x; i++)
        if(m % i == 0 && n % i == 0)
            nod = i;
    cout << m / nod << " " << n / nod;
    return 0;
}
