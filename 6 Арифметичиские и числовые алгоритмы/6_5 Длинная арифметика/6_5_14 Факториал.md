Факториал

Напишите программу, вычисляющую факториал данного числа.

Входные данные
Дано чиcло n (n ≤ 3000)

Выходные данные
Выведите факториал данного числа.

Sample Input 1:
0

Sample Output 1:
1

Sample Input 2:
5

Sample Output 2:
120

Напишите программу. Тестируется через stdin → stdout
Верно решили 111 учащихся
Из всех попыток 72% верных


# ruby
def fact(n) (1..n).inject(1) {|r,i| r*i }end

n = gets.to_i
print fact(n)


# python
result = 1
for i in range(2,int(input())+1):
    result *= i
print(result)


# java
import java.util.Scanner;
import java.math.BigInteger;

class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        
        int n = sc.nextInt();
        
        BigInteger ans = new BigInteger("1");
        
        for (int i = 1; i <= n; i++) {
            ans = ans.multiply(BigInteger.valueOf(i));
        }
        
        System.out.println(ans);
    }
}


# rust
use std::io;

fn main() {
    let mut input = String::new();
    io::stdin().read_line(&mut input).unwrap();
    let n: usize = input.trim().parse().unwrap();

    if n == 0 || n == 1 {
        println!("1");
        return;
    }

    // Используем вектор для хранения цифр числа в обратном порядке
    let mut digits: Vec<u32> = Vec::new();
    digits.push(1); // 0! или 1!

    for i in 2..=n {
        let mut carry = 0;
        for digit in digits.iter_mut() {
            let prod = *digit * i as u32 + carry;
            *digit = prod % 10;
            carry = prod / 10;
        }
        
        // Добавляем оставшийся перенос
        while carry > 0 {
            digits.push(carry % 10);
            carry /= 10;
        }
    }

    // Выводим цифры в обратном порядке
    for digit in digits.iter().rev() {
        print!("{}", digit);
    }
    println!();
}





