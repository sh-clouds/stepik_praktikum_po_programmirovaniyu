Больше предыдущего

Дан список чисел. Выведите все элементы списка, которые больше предыдущего элемента.

Входные данные
Вводится список чисел. Все числа списка находятся на одной строке.

Выходные данные
Выведите ответ на задачу.

Sample Input:
1 5 2 4 3

Sample Output:
5 4

Напишите программу. Тестируется через stdin → stdout
Верно решили 468 учащихся
Из всех попыток 52% верных


# ruby
a = gets.chomp.split().map{|j| j.to_i}
k = 0
x = a[0]
while k < a.size   
    if a[k] > x
        print "#{a[k]} "        
    end 
    x = a[k]
    k += 1
end


# python
lst = list(map(int, input().split()))
print(' '.join(str(lst[i]) for i in range(1, len(lst)) if lst[i] > lst[i - 1]))


# JS
var stdin = process.openStdin();

stdin.addListener("data", function(d) {
    let str = d.toString().split(' ');
    let result = '';
    for  (let i =1; i<str.length; i++)
        if ( +str[i-1] < +str[i]) result = result + str[i] + ' ';
    console.log(result);
  });

  
# C#
using System;

public class MainClass
{
    public static void Main()
    {
      int[] input=Array.ConvertAll(Console.ReadLine().Split(),int.Parse);
      for(int i=1;i<input.Length;i++)
      {
      Console.Write(input[i]>input[i-1]?input[i] +" ":"");
      }
    }
}


# java
import java.util.*;

class Main {
    public static void main(String[] args) {
        int[] arr = Arrays.stream(new Scanner(System.in).nextLine().split(" "))
                .mapToInt(Integer::parseInt)
                .toArray();
        for (int i = 0; i < arr.length - 1; i++) {
            if (arr[i+1] > arr[i]) {
                System.out.print(arr[i+1] + " ");
            }
        }
    }
}


# C
#include <inttypes.h>
#include <stdio.h>
#include <stdlib.h>

int main() {
    size_t N = 0;
    int64_t* array;
    int64_t num;
    while (scanf("%"SCNd64, &num) == 1) {
        N++;
        if (N == 1) array = malloc(sizeof(int64_t));
        else array = realloc(array, N * sizeof(int64_t));
        array[N - 1] = num;
    }
    
    for (size_t i = 1; i < N; i++)
        if (array[i] > array[i - 1]) printf("%"PRId64" ", array[i]);
    
    if (N) free(array);
    return 0;
}


# C++
#include <iostream>
#include <vector>

int main() {
    // put your code here
    std::vector <int> arr;
    
    int temp;
    while(std::cin >> temp)
    {
        arr.push_back(temp);
    }   
    
    for(size_t i = 1; i < arr.size(); ++i)
    {
        if(arr[i] > arr[i-1])
            std::cout << arr[i] << " ";
    }
    return 0;
}



