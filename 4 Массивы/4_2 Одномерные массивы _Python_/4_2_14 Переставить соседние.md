Переставить соседние

Переставьте соседние элементы списка (A[0] c A[1], A[2] c A[3] и т.д.). Если элементов нечетное число, то последний элемент остается на своем месте.

Входные данные
Вводится список чисел. Все числа списка находятся на одной строке.

Выходные данные
Выведите ответ на задачу.

Sample Input:
1 2 3 4 5

Sample Output:
2 1 4 3 5

Напишите программу. Тестируется через stdin → stdout
Верно решили 369 учащихся
Из всех попыток 78% верных


# ruby
a = gets.chomp.split().map{|j| j.to_i}
#a=[3, 7, 11, 5, 3, 9, 7, 2, 1]
cnt = 0
a1 = []

a.each_cons(2) do |v|   
    if cnt % 2 == 0
        a1.push(v[1]) 
        a1.push(v[0])
    end  
    cnt += 1     
end

if a.size % 2 != 0
    a1.push(a[-1]) 
end

a = a1

a.each do |i|
    print "#{i} "
end  


# python
l=list(map(int, input().split()))
l = [l[i-1:i+1][::-1] for i in range(1,len(l)+1, 2)]
ll = []
[ll.extend(i) for i in l]
print(*ll)


# JS
var stdin = process.openStdin();

stdin.addListener("data", function(d) {
    let str = d.toString().split(' ');
    
    for (let i=0; i<str.length-1; i=i+2) 
        [str[i], str[i+1]] = [str[i+1], str[i]];

    let result = '';
    for (let i=0; i<str.length; i++)
        result = result + str[i] + ' ';    
    console.log(result);
  });

  
# C#
using System;
class Program
{
    public class MainClass
    {
        public static void Main()
        {
            int[] nums = Array.ConvertAll(Console.ReadLine().Split(' '), int.Parse);
            int temp = 0;
            for (int i = 0; i < nums.Length; i += 2)
            {
                if (i < nums.Length - 1)
                {
                    temp = nums[i + 1];
                    nums[i + 1] = nums[i];
                    nums[i] = temp;
                }
            }
            foreach (int i in nums)
                Console.Write(i + " ");
        }
    }
}


# java
import java.util.*;

class Main {
    public static void main(String[] args) {
        int[] arr = Arrays.stream(new Scanner(System.in).nextLine().split(" "))
                .mapToInt(Integer::parseInt)
                .toArray();
        for (int i = 0; i < arr.length - 1; i+=2) {
            System.out.print(arr[i + 1] + " " + arr[i] + " ");
        }
        if (arr.length % 2 != 0) {
            System.out.print(arr[arr.length - 1]);
        }
    }
}


# C
#include <inttypes.h>
#include <stdio.h>
#include <stdlib.h>

int main() {
    size_t N = 0;
    int64_t* array;
    int64_t num;
    while (scanf("%"SCNd64, &num) == 1) {
        N++;
        if (N == 1) array = malloc(sizeof(int64_t));
        else array = realloc(array, N * sizeof(int64_t));
        array[N - 1] = num;
    }

    for (size_t i = 0; i < N - 1; i += 2) {
        int64_t swap = array[i];
        array[i] = array[i + 1];
        array[i + 1] = swap;
    }

    for (size_t i = 0; i < N; i++) printf("%"PRId64" ", array[i]);

    if (N) free(array);
    return 0;
}



# C++
#include<iostream>
#include<vector>
using namespace std;

int main() {
    int tmp;
  vector<int> vals;
  while(cin>>tmp){
        vals.push_back(tmp);
  }
    for(int i=0;i<(int)(vals.size()/2*2);i+=2)
        swap(vals[i],vals[i+1]);
    for (auto v:vals){
        cout <<v<<" ";
    }

  return 0;
}




