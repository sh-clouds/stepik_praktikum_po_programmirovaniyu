Больше своих соседей

Дан список чисел. Определите, сколько в этом списке элементов, которые больше двух своих соседей и выведите количество таких элементов.

Входные данные
Вводится список чисел. Все числа списка находятся на одной строке.

Выходные данные
Выведите ответ на задачу.

Sample Input 1:
1 2 3 4 5

Sample Output 1:
0

Sample Input 2:
1 5 1 5 1

Sample Output 2:
2

Напишите программу. Тестируется через stdin → stdout
Верно решили 438 учащихся
Из всех попыток 75% верных


# ruby
a = gets.chomp.split().map{|j| j.to_i}

#a = [1, 5, 1, 5, 1]
cnt = 0

a.each_cons(3) do |v|     
    if v[1]>v[0] and v[1]>v[2]
        cnt += 1       
    end       
end

p cnt


# python
lst = list(map(int, input().split()))
print(sum(lst[i] > lst[i + 1] and lst[i] > lst[i - 1] for i in range(1, len(lst) - 1)))


# JS
var stdin = process.openStdin();

stdin.addListener("data", function(d) {
    let str = d.toString().split(' ');
    let result = 0;
    for  (let i =1; i<str.length-1; i++)
        if ( +str[i] > +str[i-1] && +str[i+1] < +str[i] ) result++;
    console.log(result);
  });
  

# C#
using System;

public class MainClass
{
    public static void Main()
    {
      int[] input=Array.ConvertAll(Console.ReadLine().Split(),int.Parse);
        int count=0;
      for(int i=1;i<input.Length-1;i++)
      {          
          if(input[i]>input[i-1] && input[i]>input[i+1])
          {
              count++;
              i++;
          }
       }
        Console.WriteLine(count);
    }
}


# java
import java.util.Scanner;
class Main {
    public static void main(String[] args) {
String[]vv=new Scanner(System.in).nextLine().split(" ");int chet=0;
for(int i=2;i<vv.length;i++){
if(Integer.parseInt(vv[i-2])<Integer.parseInt(vv[i-1])&&Integer.parseInt(vv[i])<Integer.parseInt(vv[i-1])){chet++;}}
System.out.println(chet);    

    }
}


# C
#include <inttypes.h>
#include <stdio.h>
#include <stdlib.h>

int main() {
    size_t N = 0;
    int64_t* array;
    int64_t num;
    while (scanf("%"SCNd64, &num) == 1) {
        N++;
        if (N == 1) array = malloc(sizeof(int64_t));
        else array = realloc(array, N * sizeof(int64_t));
        array[N - 1] = num;
    }

    size_t count = 0;
    for (size_t i = 1; i < N - 1; i++)
        if (array[i] > array[i - 1] && array[i] > array[i + 1]) count++;

    printf("%zu", count);
    if (N) free(array);
    return 0;
}


# C++
#include <iostream>
#include <vector>

int main() {
    // put your code here
    std::vector <int> arr;
    
    int temp;
    while(std::cin >> temp)
    {
        arr.push_back(temp);
    }   
    
    int count = 0;
    for(size_t i = 1; i < arr.size() - 1; ++i)
    {
        if(arr[i] > arr[i-1] && arr[i] > arr[i+1])
            ++count;
    }
    
    std::cout << count << std::endl;
    return 0;
}




