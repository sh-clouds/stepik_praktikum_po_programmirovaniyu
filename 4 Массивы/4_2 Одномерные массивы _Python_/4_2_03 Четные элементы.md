Четные элементы

Выведите все четные элементы списка.

Входные данные
Вводится список чисел. Все числа списка находятся на одной строке.

Выходные данные
Выведите ответ на задачу.

Sample Input:
1 2 2 3 3 3 4

Sample Output:
2 2 4

Напишите программу. Тестируется через stdin → stdout
Верно решили 555 учащихся
Из всех попыток 76% верных


# ruby
a = gets.chomp.split().map{|j| j.to_i}

a.each do |i|     
    if (i%2) == 0
        print "#{i} " 
    end   
end 


# python
print(*[i for i in map(int, input().split()) if not i & 1])


# JS
var stdin = process.openStdin();

stdin.addListener("data", function(d) {
    let str = d.toString().split(' ');
    let result = '';
    for  (let i =0; i<str.length; i++)
        if (str[i] %2 === 0) result += str[i]+' ';
    console.log(result);
  });

  
# C#
using System;

public class MainClass
{
    public static void Main()
    {
      int[] input=Array.ConvertAll(Console.ReadLine().Split(),int.Parse);
      for(int i=0;i<input.Length;i++)
      {
      Console.Write(input[i]%2==0?input[i] +" ":"");
      }
    }
}


# java
import java.util.*;

class Main {
    public static void main(String[] args) {
        Arrays.stream(new Scanner(System.in).nextLine().split(" "))
                .mapToInt(Integer::parseInt)
                .filter(e -> e % 2 == 0)
                .forEachOrdered(e -> System.out.print(e + " "));
    }
}


# C 
#include <inttypes.h>
#include <stdio.h>
#include <stdlib.h>

int main() {
    size_t N = 0;
    int64_t* array;
    int64_t num;
    while (scanf("%"SCNd64, &num) == 1) {
        N++;
        if (N == 1) array = malloc(sizeof(int64_t));
        else array = realloc(array, N * sizeof(int64_t));
        array[N - 1] = num;
    }
    
    for (size_t i = 0; i < N; i++)
        if (array[i] % 2 == 0) printf("%"PRId64" ", array[i]);
    
    if (N) free(array);
    return 0;
}


# C++
#include <iostream>
#include <vector>

int main() {
    // put your code here
    std::vector <int> arr;
    
    int temp;
    while(std::cin >> temp)
    {
        arr.push_back(temp);
    }   
        
    
    for(auto &ptr : arr)
    {
        if(ptr % 2 == 0)
            std::cout << ptr << " ";
    }
    return 0;
}


