Юлианский календарь

По заданному числу n от 1 до 365 определите, на какое число какого месяца приходится день невисокосного года с номером n.

Входные данные
Дано одно целое число n.

Выходные данные
Программа должна вывести два числа: число месяца (от 1 до 31) и номер месяца (от 1 до 12), соответствующие  дню  с номером n.

Sample Input:
90

Sample Output:
31 3

Напишите программу. Тестируется через stdin → stdout
Верно решили 193 учащихся
Из всех попыток 59% верных


# ruby
m = [31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31]
n = gets.to_i
sum = 0

0.upto(m.size-1) do |i|
    if sum + m[i] > n-1
        print "#{n-sum} #{i+1}"
        break
    else        
        sum += m[i]
    end
end 

# python
from datetime import datetime, timedelta
future_date = datetime(1, 12, 31) + timedelta(days=int(input()))
print(future_date.day, future_date.month)


# JS
var stdin = process.openStdin();

stdin.addListener("data", function(d) {
    let n = +d.toString();
    let new_data = new Date(n*24*60*60*1000-1);
    console.log(new_data.getDate() +' '+ (new_data.getMonth()+1));
  });


# C#
using System;

public class MainClass
{
    public static void Main()
    {
        int n=int.Parse(Console.ReadLine());
        int[] months={0,31,28,31,30,31,30,31,31,30,31,30,31};
        for(int i=0;i<13;i++)
        {
            n-=months[i];
            if(n<=0)
            {
            Console.WriteLine($"{months[i]+n} {i}");
            break;
            }
        }
    }
}


# java
import java.time.LocalDate;
import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
        Scanner input = new Scanner(System.in);
        int number = Integer.parseInt(input.nextLine());

        LocalDate date = LocalDate.ofYearDay(number, number);

        if(date.lengthOfYear() == 366 && date.compareTo(LocalDate.of(number, 2, 29)) >= 0) {
            date = date.plusDays(1);
        }

        System.out.printf("%d %d", date.getDayOfMonth(), date.getMonthValue());
    }
}


# C
#include <stdio.h>

int main(int argc, char **argv)
{
	int d;
	int m = 0;
	int a[13] = {0, 31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31};

	scanf("%d", &d);

	while (a[m] < d) {
		d -= a[m++];
	}

	printf("%d %d\n", d, m);

	return 0;
}



# C++
#include <iostream>

using namespace std;


int main()
{
int month[]={31,28,31,30,31,30,31,31,30,31,30,31};
int n,i;
cin>>n;
for(i=0;i<12;i++){
    if(n-month[i]>0){
        n-=month[i];
    }
    else
        break;
}
cout<<n<<" "<<i+1<<endl;

return 0;
}



