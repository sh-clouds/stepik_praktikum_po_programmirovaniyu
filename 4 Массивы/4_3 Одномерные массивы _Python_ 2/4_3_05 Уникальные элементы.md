Уникальные элементы

Дан список. Выведите те его элементы, которые встречаются в списке только один раз. Элементы нужно выводить в том порядке, в котором они встречаются в списке.

Входные данные
Вводится список чисел. Все числа списка находятся на одной строке.

Выходные данные
Выведите ответ на задачу.

Sample Input:
1 2 2 3 3 3

Sample Output:
1



# ruby
a = gets.chomp.split().map{|j| j.to_i}
#a = [2, 3, 4, 5]

a.each do |v0|
    c = 0
    a.each do |v1|
        if v0==v1
            c += 1
        end
    end
    print "#{v0} " if c == 1   
end 



# python
d = {}
for i in map(int, input().split()):
    d[i] = d.get(i, 0) + 1
lst = [key for key, value in d.items() if value == 1]
print(*lst)



# JS
var stdin = process.openStdin();

stdin.addListener("data", function(d) {
    let str = d.toString().split(' ');
    let result = '';
    let flag = true;
    
    for (let i=0; i<str.length; i++) {
        if (str[i] === null) continue;
        for (let j=i+1; j<str.length; j++) {
            if ( str[i] === str[j] ) {
            flag = false;
            str[j] = null;
            }            
        }
        if (flag) result = result + str[i] + ' ';
        flag = true;    
    }     
    console.log(result);
  });

  
# C#
using System;
using System.Linq;

public class MainClass
{
    public static void Main()
    {
        var arr=Console.ReadLine().Split();
        var hash=arr.ToHashSet();
        foreach(string i in hash)
        {
            Console. Write(Array.IndexOf(arr,i)==Array.LastIndexOf(arr,i)?i+" ":"");
        }
    }
}

# java
import java.util.Scanner;
class Main {
    public static void main(String[] args) {
String []vv=new Scanner(System.in).nextLine().split(" ");
for(int i=0,chet=0;i<vv.length;i++){
for(int j=0;j<vv.length;j++){
if(i!=j&&vv[i].equals(vv[j])){chet++;}}
if(chet==0){System.out.print(vv[i]+" ");}
chet=0;}    

    }
}



# C
#include <inttypes.h>
#include <stdio.h>
#include <stdlib.h>

void input(int64_t** arr, size_t* N, size_t* K, int64_t* X);
void output(int64_t** arr, size_t N);
void pop_item(int64_t** arr, size_t* N);
void rem_item(int64_t** arr, size_t* N, size_t K);
void append_item(int64_t** arr, size_t* N, size_t K, int64_t X);
size_t count_item(int64_t** arr, size_t N, int64_t X);

int main() {
    size_t N = 0, K = 0;
    int64_t* array;
    int64_t X;
    input(&array, &N, &K, &X);

    for (size_t i = 0; i < N; i++)
        if (count_item(&array, N, array[i]) == 1) printf("%"PRId64" ", array[i]);

    //output(&array, N);
    if (N) free(array);
    return 0;
}

size_t count_item(int64_t** arr, size_t N, int64_t X) {
    size_t count = 0;
    for (size_t i = 0; i < N; i++)
        if ((*arr)[i] == X) count++;
    return count;
}

void append_item(int64_t** arr, size_t* N, size_t K, int64_t X) {
    (*N)++;
    (*arr) = realloc((*arr), (*N) * sizeof(int64_t));
    for (size_t i = (*N) - 1; i > K; i--) (*arr)[i] = (*arr)[i - 1];
    (*arr)[K] = X;
}

void rem_item(int64_t** arr, size_t* N, size_t K) {
    for (size_t i = K + 1; i < (*N); i++) (*arr)[i - 1] = (*arr)[i];
    pop_item(&(*arr), &(*N));
}

void pop_item(int64_t** arr, size_t* N) {
    (*N)--;
    (*arr) = realloc((*arr), (*N) * sizeof(int64_t));
}

void output(int64_t** arr, size_t N) {
    for (size_t i = 0; i < N; i++) {
        printf("%"PRId64, (*arr)[i]);
        if (i != N - 1) printf(" ");
    }
}

void input(int64_t** arr, size_t* N, size_t* K, int64_t* X) {
    int64_t num;
    while (scanf("%"SCNd64, &num) == 1) {
        (*N)++;
        if ((*N) == 1) (*arr) = malloc(sizeof(int64_t));
        else (*arr) = realloc((*arr), (*N) * sizeof(int64_t));
        (*arr)[(*N) - 1] = num;
    }
    (*X) = 0;
    (*K) = 0;
}


# C++
#include <iostream>
#include <vector>
#include <algorithm>
#include <iterator>

int main() {
    // put your code here
    std::vector  <int> arr;
    
    int temp;
    while(std::cin >> temp)
    {
        arr.push_back(temp);
    }
     
    for(auto &&ptr : arr)
    {
        std::vector <int> :: iterator it = std::find(arr.begin(), arr.end(), ptr);
        if(std::find(it + 1, arr.end(), ptr) == arr.end())
           std::cout << ptr << " ";
    }
    return 0;
}