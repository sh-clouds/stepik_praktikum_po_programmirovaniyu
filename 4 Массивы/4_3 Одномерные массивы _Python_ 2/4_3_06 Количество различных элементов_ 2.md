Количество различных элементов - 2

Дан список. Посчитайте, сколько в нем различных элементов, не изменяя самого списка.

Входные данные
Вводится список чисел. Все числа списка находятся на одной строке.

Выходные данные
Выведите ответ на задачу.

Sample Input:
3 2 1 2 3

Sample Output:
3

Напишите программу. Тестируется через stdin → stdout
Верно решили 383 учащихся
Из всех попыток 71% верных


# ruby
a = gets.chomp.split().map{|j| j.to_i}
#a = [2, 3, 4, 5]
a1 = []

a.each do |v|
    a1.push(v) if a1.include?(v) == false
end  
p a1.size 


# python
print(len(set(input().split())))


# JS
var stdin = process.openStdin();

stdin.addListener("data", function(d) {
    let my_set = new Set(d.toString().split(' '));
    console.log(my_set.size);
  });


# C#
using System;
using System.Linq;
public class MainClass
{
    public static void Main()
    {
            Console.WriteLine(Console.ReadLine().Split(' ').Distinct().Count());
    }
}


# java
import java.util.Scanner;
class Main {
    public static void main(String[] args) {
String[]vv=new Scanner(System.in).nextLine().split(" ");
int dlina=vv.length,min=Integer.MAX_VALUE,max=Integer.MIN_VALUE,fin=0;
int []vas=new int[dlina];        
for(int i=0;i<vv.length;i++){
vas[i]=Integer.parseInt(vv[i]);
if(max<vas[i]){max=vas[i];}
if(min>vas[i]){min=vas[i];}}
for(int i=min,chet=0;i<=max;i++){     
for(int j=0;j<dlina;j++){
if(i==vas[j]){chet++;}}
if(chet>0){fin++;}
chet=0;}
System.out.println(fin);        
    }
}


# C
#include <inttypes.h>
#include <stdio.h>
#include <stdlib.h>

void input(int64_t** arr, size_t* N, size_t* K, int64_t* X);
void output(int64_t** arr, size_t N);
void pop_item(int64_t** arr, size_t* N);
void rem_item(int64_t** arr, size_t* N, size_t K);
void append_item(int64_t** arr, size_t* N, size_t K, int64_t X);
size_t count_item(int64_t** arr, size_t N, int64_t X);

int main() {
    size_t N = 0, K = 0;
    int64_t* array;
    int64_t X;
    input(&array, &N, &K, &X);

    for (size_t i = 0; i < N; i++)
        if (count_item(&array, N, array[i]) == 1) printf("%"PRId64" ", array[i]);

    //output(&array, N);
    if (N) free(array);
    return 0;
}

size_t count_item(int64_t** arr, size_t N, int64_t X) {
    size_t count = 0;
    for (size_t i = 0; i < N; i++)
        if ((*arr)[i] == X) count++;
    return count;
}

void append_item(int64_t** arr, size_t* N, size_t K, int64_t X) {
    (*N)++;
    (*arr) = realloc((*arr), (*N) * sizeof(int64_t));
    for (size_t i = (*N) - 1; i > K; i--) (*arr)[i] = (*arr)[i - 1];
    (*arr)[K] = X;
}

void rem_item(int64_t** arr, size_t* N, size_t K) {
    for (size_t i = K + 1; i < (*N); i++) (*arr)[i - 1] = (*arr)[i];
    pop_item(&(*arr), &(*N));
}

void pop_item(int64_t** arr, size_t* N) {
    (*N)--;
    (*arr) = realloc((*arr), (*N) * sizeof(int64_t));
}

void output(int64_t** arr, size_t N) {
    for (size_t i = 0; i < N; i++) {
        printf("%"PRId64, (*arr)[i]);
        if (i != N - 1) printf(" ");
    }
}

void input(int64_t** arr, size_t* N, size_t* K, int64_t* X) {
    int64_t num;
    while (scanf("%"SCNd64, &num) == 1) {
        (*N)++;
        if ((*N) == 1) (*arr) = malloc(sizeof(int64_t));
        else (*arr) = realloc((*arr), (*N) * sizeof(int64_t));
        (*arr)[(*N) - 1] = num;
    }
    (*X) = 0;
    (*K) = 0;
}


# C++
#include <iostream>
#include <vector>
#include <set>

int main() {
    // put your code here
    std::vector  <int> arr;
    
    int temp;
    while(std::cin >> temp)
    {
        arr.push_back(temp);
    }
    
    std::cout << std::set <int> (arr.begin(), arr.end()).size() << std::endl;
    
    return 0;
}