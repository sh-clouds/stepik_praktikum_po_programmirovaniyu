Количество совпадающих пар

Дан список чисел. Посчитайте, сколько в нем пар элементов, равных друг другу. Считается, что любые два элемента, равные друг другу образуют одну пару, которую необходимо посчитать.

Входные данные
Вводится список чисел. Все числа списка находятся на одной строке.

Выходные данные
Выведите ответ на задачу.

Sample Input 1:
1 2 3 2 3

Sample Output 1:
2

Sample Input 2:
1 1 1 1 1

Sample Output 2:
10

Напишите программу. Тестируется через stdin → stdout
Верно решили 369 учащихся
Из всех попыток 77% верных


# ruby
a = gets.chomp.split().map{|j| j.to_i}
#a = [1, 1, 1, 1, 1]
c = 0

a.each do |v0|
    c -= 1
    a.each do |v1|
        if v0==v1
            c += 1
        end
    end
    #puts c    
end    
                       
puts c/2


# python
lst = list(map(int, input().split()))
print(sum(lst[i] == lst[j] for i in range(len(lst)) for j in range(i + 1, len(lst))))


# JS
var stdin = process.openStdin();

stdin.addListener("data", function(d) {
    let str = d.toString().split(' ');
    let result = 0;
    for (let i=0; i<str.length-1; i++)
        for (let j=i+1; j<str.length; j++)
            if ( +str[i] === +str[j] ) result++;
    console.log(result);
  });

  
# C#
using System;
using System.Linq;

public class MainClass
{
    public static void Main()
    {
            var arr = Console.ReadLine().Split().ToList();
            int count = 0;
            for (int i = 0; i < arr.Count; i++)
            {
                for (int y = i+1; y < arr.Count; y++)
                {
                    count += arr[i] == arr[y] ? 1 : 0;
                }
            }
            Console.WriteLine(count);
    }
}


# java
import java.util.Scanner;
import java.util.Arrays;
class Main {
    public static void main(String[] args) {
String []vv=new Scanner(System.in).nextLine().split(" ");
int dlina=vv.length;int []Vas=new int[vv.length];
for(int i=0;i<vv.length;i++){
Vas[i]=Integer.parseInt(vv[i]);}
int chet=0;
for(int i=0;i<Vas.length-1;i++){
    for(int j=i+1;j<Vas.length;j++){
if(Vas[i]==Vas[j]){chet++;}}}
System.out.println(chet);  

    }
}


# C
#include <inttypes.h>
#include <stdio.h>
#include <stdlib.h>

void input(int64_t** arr, size_t* N, size_t* K, int64_t* X);
void output(int64_t** arr, size_t N);
void pop_item(int64_t** arr, size_t* N);
void rem_item(int64_t** arr, size_t* N, size_t K);
void append_item(int64_t** arr, size_t* N, size_t K, int64_t X);

int main() {
    size_t N = 0, K = 0;
    int64_t* array;
    int64_t X;
    input(&array, &N, &K, &X);

    size_t count = 0;
    for (size_t i = 0; i < N - 1; i++)
        for (size_t j = i + 1; j < N; j++)
            if (array[i] == array[j]) count++;

    printf("%zu", count);
    //output(&array, N);
    if (N) free(array);
    return 0;
}

void append_item(int64_t** arr, size_t* N, size_t K, int64_t X) {
    (*N)++;
    (*arr) = realloc((*arr), (*N) * sizeof(int64_t));
    for (size_t i = (*N) - 1; i > K; i--) (*arr)[i] = (*arr)[i - 1];
    (*arr)[K] = X;
}

void rem_item(int64_t** arr, size_t* N, size_t K) {
    for (size_t i = K + 1; i < (*N); i++) (*arr)[i - 1] = (*arr)[i];
    pop_item(&(*arr), &(*N));
}

void pop_item(int64_t** arr, size_t* N) {
    (*N)--;
    (*arr) = realloc((*arr), (*N) * sizeof(int64_t));
}

void output(int64_t** arr, size_t N) {
    for (size_t i = 0; i < N; i++) {
        printf("%"PRId64, (*arr)[i]);
        if (i != N - 1) printf(" ");
    }
}

void input(int64_t** arr, size_t* N, size_t* K, int64_t* X) {
    int64_t num;
    while (scanf("%"SCNd64, &num) == 1) {
        (*N)++;
        if ((*N) == 1) (*arr) = malloc(sizeof(int64_t));
        else (*arr) = realloc((*arr), (*N) * sizeof(int64_t));
        (*arr)[(*N) - 1] = num;
    }
    (*X) = 0;
    (*K) = 0;
}



# C++
#include <iostream>
#include <vector>
#include <algorithm>
#include <iterator>

int main() {
    // put your code here
    std::vector  <int> arr;
    
    int temp;
    while(std::cin >> temp)
        arr.push_back(temp);
    
    temp = 0;
    for(size_t i = 0; i < arr.size(); ++i)
    {
        std::vector <int> :: iterator it = arr.begin() + i + 1;
        temp += std::count(it, arr.end(), arr[i]);
    }
    
    std::cout << temp << std::endl;
    return 0;
}



