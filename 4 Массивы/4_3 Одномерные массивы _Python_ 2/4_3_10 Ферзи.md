Ферзи

Известно, что на доске 8×8 можно расставить 8 ферзей так, чтобы они не били друг друга. Вам дана расстановка 8 ферзей на доске, определите, есть ли среди них пара бьющих друг друга.

Входные данные
Программа получает на вход восемь пар чисел, каждое число от 1 до 8 - координаты 8 ферзей.

Выходные данные
Если ферзи не бьют друг друга, выведите слово NO, иначе выведите YES.

Sample Input 1:
1 7
2 4
3 2
4 8
5 6
6 1
7 3
8 5

Sample Output 1:
NO



Sample Input 2:
1 8
2 7
3 6
4 5
5 4
6 3
7 2
8 1

Sample Output 2:
YES

Напишите программу. Тестируется через stdin → stdout

Верно решили 245 учащихся
Из всех попыток 49% верных


# put your ruby code here
def queen_shot_other_figure? x1,y1,x2,y2
    ((x1-x2).abs == (y1-y2).abs) || (x1==x2 || y1==y2) ? true : false
end 

a = []
8.times do
    a.push((gets.chomp.split().map{|j| j.to_i}))
end    

#a = [[1, 7], [2, 4], [2, 2], [4, 8], [5, 6], [6, 1], [7, 3], [8, 5]]
#test 8
#a = [[8, 3], [2, 2], [7, 5], [4, 1], [1, 6], [3, 7], [6, 8], [5, 4]] 
#print a
#print "\n" 

c = 0    #count shots

a.each do |v0|
    c -= 1
    a.each do |v1|
        if queen_shot_other_figure? v0[0], v0[1], v1[0], v1[1]
            c += 1
        end
    end
    #puts c    
end   

#puts c/2  
puts c/2 == 0 ? "NO" : "YES"


# python
def f(s):
    for i in range(len(s)):
        for j in range(i + 1, len(s)):
            if s[i].is_hits(s[j]):
                return 'YES'
    return 'NO'


class Queen:
    def __init__(self, x: int, y: int) -> None:
        self.x = x
        self.y = y
    
    def is_hits(self, other):
        return self.x == other.x or self.y == other.y or (abs(self.x - other.x) == abs(self.y - other.y))
    

print(f([Queen(*map(int, input().split())) for _ in range(8)]))



# JS
let arr = require('fs').readFileSync(0, 'utf8').split('\n').map(w => w.split(' ').map(Number));

let is_kick = ([x1, y1], [x2, y2]) => (Math.abs(x2 - x1) == Math.abs(y2 - y1)) || x1 == x2 || y1 == y2;

let badaboom = 'NO';
outer:
for (let i = 0; i < 7; i++) {
    for (let j = i+1; j < 8; j++) {
        if (is_kick(arr[i], arr[j])) {
            badaboom = 'YES';
            break outer;
        }
    }   
}

console.log(badaboom);




# C#
using System;
using System.Collections.Generic;
using System.Linq;

public class MainClass
{
    public static void Main()
    {
        bool isFight = false;
        var queens = new List<Queen>();
        for (int i = 0; i < 8; i++)
        {
            queens.Add(new Queen(Console.ReadLine()));
            for (int n = 0; n < queens.Count - 1; n++)
            {
                isFight = Math.Abs(queens[i].x - queens[n].x) == Math.Abs(queens[i].y - queens[n].y);
                if (isFight) break;
            }
            if (isFight) break;
        }
        if (!isFight)
        {
        isFight = queens.Sum(p => p.x) != 36 || queens.Sum(p => p.y) != 36;
        }
        Console.WriteLine(isFight ? "YES" : "NO");
    }
}
public class Queen
{
    public int x;
    public int y;
    public Queen(string str)
    {
        int[] input = Array.ConvertAll(str.Split(), int.Parse);
        x = input[0];
        y = input[1];
    }
}


# java
import java.util.Scanner;
class Main {
    public static void main(String[] args) {
Scanner v =new Scanner(System.in);
int[]vv=new int[8*2];boolean ff=false;
for(int i=0;i<vv.length;i++){
vv[i]=v.nextInt();}
for(int i=0,x1=0,y1=0;i<vv.length;i+=2){
x1=vv[i];y1=vv[i+1];   
for(int j=0,x2=0,y2=0;j<vv.length;j+=2){
x2=vv[j];y2=vv[j+1];
if(i!=j&&(x1==x2||y1==y2||x1+y2==x2+y1||x1+y1==x2+y2)){ff=true;break;}}    
if(ff){break;}}
System.out.println(ff?"YES":"NO");
    }
}


# C 
#include <inttypes.h>
#include <stdio.h>
#include <stdlib.h>

int main() {
    size_t N = 8;
    uint8_t X[N], Y[N], kick = 0;

    for (size_t i = 0; i < N; i++) {
        scanf("%"SCNu8, &X[i]);
        scanf("%"SCNu8, &Y[i]);
    }

    for (size_t i = 0; i < N - 1; i++)
        for (size_t j = i + 1; j < N - 1; j++)
            if ((X[i] == X[j]) || (Y[i] == Y[j]) || (abs(X[i] - X[j]) == abs(Y[i] - Y[j]))) {
                kick = 1;
                break;
            }

    printf("%s", kick ? "YES" : "NO");
    return 0;
}


# C++
#include <iostream>
#include <cmath>

const int board = 8;

int main() {
    // put your code here
    int x[board], y[board];
    
    for(size_t i = 0; i < board; ++i)
    {
        std::cin >> x[i] >> y[i];
    }
    
    for(size_t i = 0; i < board; ++i)
    {
        for(size_t j = i + 1; j < board; ++j)
        {
            if(x[i] == x[j] || y[i] == y[j] || abs(x[i] - x[j]) == abs(y[i] - y[j]))
            {
                std::cout << "YES" << std::endl;
                return 0;
            }
        }
    }    
    
    std::cout << "NO" << std::endl;
    return 0;
}