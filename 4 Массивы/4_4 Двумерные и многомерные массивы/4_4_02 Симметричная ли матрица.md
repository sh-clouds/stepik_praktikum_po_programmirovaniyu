Симметричная ли матрица?

Проверьте, является ли двумерный массив симметричным относительно главной диагонали. Главная диагональ — та, которая идёт из левого верхнего угла двумерного массива в правый нижний.

Входные данные
Программа получает на вход число n, n ≤ 100, являющееся числом строк и столбцов в массиве. Далее во входном потоке идет n строк по nчисел, являющихся элементами массива.

Выходные данные
Программа должна выводить слово yes для симметричного массива и слово no для несимметричного.

Sample Input:
3
0 1 2
1 5 3
2 3 4

Sample Output:
yes

Напишите программу. Тестируется через stdin → stdout
Верно решили 396 учащихся
Из всех попыток 51% верных


# ruby
n = gets.to_i
a = []

0.upto(n-1) do |x|   
    a.push(gets.chomp.split(' ').map { |j| j.to_i }) 
end
f = true;

0.upto(n-1) do |x|
    0.upto(n-1) do |y| 
        if x < y
            if a[x][y] != a[y][x]
                f = false
                break
            end    
        end
    end     
end

puts f == true ? "yes" : "no"  


# python
def f(m: list[list[int]]) -> str:
    for i in range(len(m)):
        for j in range(len(m)):
            if m[i][j] != m[j][i]:
                return 'no'
    return 'yes'


print(f([list(map(int, input().split())) for _ in range(int(input()))]))

    
# JS
let arr = require('fs').readFileSync(0, 'utf8').split('\n').slice(1);

arr = arr.map(z => z.split(' '));

const transpose = matrix => matrix[0].map((_, i) => matrix.map(row => row[i]));
const  trarr = transpose(arr);

console.log(!(arr > trarr || arr < trarr) ? 'yes' : 'no')


# C#
using System;

public class MainClass
{
    public static void Main()
    {
        int n = int.Parse(Console.ReadLine());
        int[,] arr = new int[n, n];
        bool symmetric = true;

        for (int i = 0; i < n; i++)
        {
            int[] line = Array.ConvertAll(Console.ReadLine().Split(),int.Parse);
            for (int j = 0; j < n; j++)
            {
                arr[i, j] = line[j];
                if (i>0 && j<i)
                {                    
                    symmetric = arr[i, j] == arr[j, i];
                }                
                if (!symmetric) break;  
            }
            if (!symmetric) break;            
        }
        Console.WriteLine(symmetric?"yes":"no");
    }
}


# java
import java.util.Scanner;
class Main {
    public static void main(String[] args) {
Scanner v=new Scanner(System.in);int n=v.nextInt();boolean ff=true;
int [][]vas=new int[n][n];
for(int i=0;i<n;i++){for(int j=0;j<n;j++){vas[i][j]=v.nextInt();}}
for(int i=0;i<n;i++){
    for(int j=0;j<n;j++){if(i!=j&&vas[i][j]!=vas[j][i]){ff=false;}}
}
System.out.println(ff?"yes":"no");        
        
    }
}


# C
#include <inttypes.h>
#include <stdio.h>
#include <stdlib.h>

void input_count(size_t* N);
int8_t init_matrix(int64_t*** arr, size_t N, size_t M);
void free_matrix(int64_t*** arr, size_t N);
void echo_matrix(int64_t*** arr, size_t N, size_t M);
void input_matrix(int64_t*** arr, size_t N, size_t M);
void fill_matrix(int64_t*** arr, size_t N, size_t M);
int8_t check_symmetry(int64_t*** arr, size_t N, size_t M);

int main() {
    size_t N, M;
    input_count(&N);
    M = N;
    int64_t** matrix = NULL;
    if (N && M && init_matrix(&matrix, N, M)) {
        input_matrix(&matrix, N, M);
        printf("%s", check_symmetry(&matrix, N, M) ? "yes" : "no");
        free_matrix(&matrix, N);
    }
    return 0;
}

int8_t check_symmetry(int64_t*** arr, size_t N, size_t M) {
    int8_t result = 1;
    for (size_t i = 0; i < N; i++)
        for (size_t j = 0; j < M; j++) {
            if (i == j) continue;
            if ((*arr)[i][j] == (*arr)[j][i]) continue;
            result = 0;
            break;
        }
    return result;
}

void fill_matrix(int64_t*** arr, size_t N, size_t M) {
    for (size_t i = 0; i < N; i++)
        for (size_t j = 0; j < M; j++)
            (*arr)[i][j] = (i + j == N - 1) + (i + j > N - 1) * 2;
}

void input_matrix(int64_t*** arr, size_t N, size_t M) {
    for (size_t i = 0; i < N; i++)
        for (size_t j = 0; j < M; j++)
            scanf("%"SCNd64, &((*arr)[i][j]));
}

void echo_matrix(int64_t*** arr, size_t N, size_t M) {
    for (size_t i = 0; i < N; i++) {
        for (size_t j = 0; j < M; j++) {
            printf("%"PRId64, (*arr)[i][j]);
            printf("%s", j < M - 1 ? " " : "\n");
        }
    }
}

void free_matrix(int64_t*** arr, size_t N) {
    for (size_t i = 0; i < N; i++) free((*arr)[i]);
    free(*arr);
}

int8_t init_matrix(int64_t*** arr, size_t N, size_t M) {
    int8_t result = 0;
    *arr = malloc(N * sizeof(int64_t*));
    if (*arr != NULL) {
        result = 1;
        for (size_t i = 0; i < N; i++) {
            (*arr)[i] = malloc(M * sizeof(int64_t));
            if ((*arr)[i] == NULL) {
                result = 0;
                free_matrix(&(*arr), i);
                break;
            }
        }
    }
    return result;
}

void input_count(size_t* N) {
    int64_t num;
    *N = (scanf("%zu", &num) == 1 && num > 0) ? num : 0;
}



# C++
#include <iostream>

int main() {
    // put your code here
    size_t size = 0;
    std::cin >> size;
    
    int arr [size][size];
    
    for(auto &&ptr : arr)
        for(auto &&sec :ptr)
            std::cin >> sec;
    
    for(size_t i = 0; i < size; ++i)
        for(size_t j = 0; j < size; ++j)
            if(i != j && arr[i][j] != arr[j][i])
            {
                std::cout << "no" << std::endl;
                return 0;
            }
    
    std::cout << "yes" << std::endl;
    return 0;
}

