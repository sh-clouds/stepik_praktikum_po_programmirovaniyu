Удалить каждый третий символ

Дана строка. Удалите из нее все символы, чьи индексы делятся на 3.

Входные данные
Вводится строка.

Выходные данные
Выведите ответ на задачу.

Sample Input:
Python

Sample Output:
yton

Напишите программу. Тестируется через stdin → stdout
Верно решили 394 учащихся
Из всех попыток 62% верных


# ruby
s = gets
s_out = ''
#s = 'Python'
#s = 'yton'
i = 0;
s.each_char do |c|
    if (i%3 != 0) 
        s_out += c        
    end    
    i += 1
end    

puts s_out


# python
print(''.join(v for i, v in enumerate(input()) if i % 3))


# JS
let s = require('fs').readFileSync(0, 'utf8').split('');
s = s.filter((_, index) => index%3).join('');
console.log(s);


# C#
using System;

public class MainClass
{
    public static void Main()
    {
        string str=Console.ReadLine();
        for(int i=0;i<str.Length;i++)
        {
            if(i%3==0){}
            else
               {
                   Console.Write(str[i]);
                }
        }
    }
}


# java
import java.util.Scanner;
class Main {
    public static void main(String[] args) {
        char [] letters = new Scanner(System.in).nextLine().toCharArray();
        for (int i = 0; i < letters.length; i++)
            if (i%3 != 0) System.out.print(letters[i]);
    }
}


# C++
#include <iostream>
#include <string>
#include <vector>
#include <sstream>

int main() {
    // put your code here
    std::vector <std::string> strVec;
    std::string str, copy;
    std::getline(std::cin, str);
    
    for(const auto &ptr : str){
        if((int)ptr < 0){
            copy += ptr;
        } else {
            if(copy[0]){
                strVec.push_back(copy);
                copy.clear();
            }
            
            std::stringstream ss;
            ss << ptr;
            std::string s = ss.str();
            strVec.push_back(s);
        }
    }
    
    if(copy[0]){
        strVec.push_back(copy);
        copy.clear();
    }
    
    for(int i = 0; i < strVec.size(); i += 2){
        auto iter = strVec.begin() + i;
        strVec.erase(iter);
    }
    
    for(const auto &ptr : strVec){
        std::cout << ptr;
    }
    return 0;
}

