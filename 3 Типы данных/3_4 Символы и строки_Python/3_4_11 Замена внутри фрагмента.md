Замена внутри фрагмента

Дана строка. Замените в этой строке все появления буквы h на букву H, кроме первого и последнего вхождения.

Входные данные
Вводится строка.

Выходные данные
Выведите ответ на задачу.

Sample Input:
In the hole in the ground there lived a hobbit

Sample Output:
In the Hole in tHe ground tHere lived a hobbit

Напишите программу. Тестируется через stdin → stdout
Верно решили 382 учащихся
Из всех попыток 80% верных


# ruby
s = gets
#s = 'In the hole in the ground there lived a hobbit'
#s = 'In the Hole in tHe ground tHere lived a hobbit'
s_small = s.downcase
c = 'h'
s1 = s_small.index(c)
s2 = s_small.reverse.index(c)
s2 = s_small.size - 1 - s2
#puts s1, s2
puts s[0...s1+1] + s[s1+1..s2-1].gsub("h", "H") + s[s2..]


# python
s = input()
left, right = s.find('h'), s.rfind('h')
print(s[:left + 1] + s[left + 1:right].replace('h', 'H') + s[right:])


# JS
let s = require('fs').readFileSync(0, 'utf8');

let [first_ind, last_ind] = [s.indexOf('h'), s.lastIndexOf('h')];

let left = s.slice(0, first_ind+1);
let center = s.slice(first_ind+1, last_ind).replaceAll('h', 'H');
let right = s.slice(last_ind);

console.log(left + center + right);


# C#
using System;

public class LetterReplacing
{
    public static void Main()
    {
        char[] chars = Console.ReadLine().ToCharArray();
        
        bool hasFirstChar = false;
        int lastIndex = default;
        for (int i = 0; i < chars.Length; i++)
        {
            if (chars[i] != 'h') continue;
            if (!hasFirstChar) { hasFirstChar = true; continue; }
            
            chars[i] = 'H';
            lastIndex = i;
        }
        chars[lastIndex] = 'h';
        
        Console.WriteLine(chars);
    }
}


# java
import java.util.Scanner;

class Main {
    public static void main(String[] args) {
        Scanner input = new Scanner(System.in);
        String data = input.nextLine();

        int start = data.indexOf("h");
        int end = data.lastIndexOf("h");

        String theFirst = data.substring(0, start + 1);
        String theSecond = data.substring(start + 1, end).replace("h", "H");
        String theThird = data.substring(end);

        System.out.printf("%s%s%s", theFirst, theSecond, theThird);
    }
}


# C++
#include <iostream>

int main() {
    std::string s;
    std::getline(std::cin, s);
    
    int pos;
    while ((pos = s.find("h")) != std::string::npos) {
        s.replace(pos, 1, "H");
    }
    if (s.find("H") != -1) {
        s[s.find("H")] = 'h';
        s[s.rfind("H")] = 'h';
    }
    
    std::cout << s << "\n";
    return 0;
}


# rust
use std::io::{self, Read};

fn main() {
    // Read the entire input as a single string
    let mut input = String::new();
    io::stdin()
        .read_to_string(&mut input)
        .expect("Failed to read from stdin");

    // Remove possible trailing newline characters
    let s = input.trim_end_matches(&['\n', '\r'][..]);

    // Find the first and last occurrence of 'h'
    let left_opt = s.find('h');
    let right_opt = s.rfind('h');

    // If there is at least one 'h', rebuild the string according to the Python logic
    if let (Some(left), Some(right)) = (left_opt, right_opt) {
        // Build the resulting string
        let mut result = String::new();

        // Part up to and including the first 'h'
        result.push_str(&s[..=left]);

        // Middle part (between the first and last 'h') with 'h' → 'H'
        if left + 1 <= right {
            let middle = &s[left + 1..right];
            result.push_str(&middle.replace('h', "H"));
        }

        // Tail part from the last 'h' to the end
        result.push_str(&s[right..]);

        println!("{}", result);
    } else {
        // No 'h' found – output the original string unchanged
        println!("{}", s);
    }
}


