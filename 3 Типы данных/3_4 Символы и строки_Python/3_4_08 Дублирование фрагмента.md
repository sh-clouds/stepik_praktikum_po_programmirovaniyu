Дублирование фрагмента

Дана строка, в которой буква h встречается как минимум два раза. Повторите последовательность символов, заключенную между первым и последним появлением буквы h два раза, сами буквы h повторять не надо.

Имейте ввиду, в тексте могут встречаться заглавные буквы H.

Входные данные
Вводится строка.

Выходные данные
Выведите ответ на задачу.

Sample Input:
In the hole in the ground there lived a hobbit

Sample Output:
In the hole in the ground there lived a e hole in the ground there lived a hobbit

Напишите программу. Тестируется через stdin → stdout
Верно решили 308 учащихся
Из всех попыток 62% верных


# ruby
s = gets
s_small = s.downcase
#s = 'In the hole in the ground there lived a Hobbit'
#s = 'In the hole in the ground there lived a e hole in the ground there lived a hobbit'
c = 'h'
s1 = s_small .index(c)
s2 = s_small .reverse.index(c)
s2 = s_small .size - 1 - s2
#puts s1, s2
puts s[0...s1+1] + s[s1+1..s2-1] +  s[s1+1..s2-1] + s[s2..]


# python
s = input()
i_1, i_2 = s.lower().find('h'), s.rfind('h')
print(s.replace(s[i_1 + 1 : i_2], s[i_1 + 1 : i_2] * 2))


# JS
const txt = require('fs').readFileSync(0, 'utf8');
let tmp = txt.toLowerCase();

let first_h = tmp.indexOf('h');
let last_h = tmp.lastIndexOf('h');

let left = txt.slice(0, first_h+1);
let center = txt.slice(first_h+1, last_h).repeat(2);
let right = txt.slice(last_h);

console.log(left + center + right);


# C#
using System;

public class FragmentDuplication
{
    public static void Main()
    {
        string text = Console.ReadLine();
        
        // int first = text.IndexOf('h');
        // int last = text.LastIndexOf('h');
        int first = text.ToLower().IndexOf('h');
        int last = text.ToLower().LastIndexOf('h');
        
        string sequence = text.Substring(first + 1, last - first - 1);
        string result = text.Insert(last, sequence);
        
        Console.WriteLine(result);
    }
}

# java
import java.util.Scanner;
class Main {
    public static void main(String[] args) {
Scanner v = new Scanner(System.in);
String gg=v.nextLine();int perv=0,posl=0,chet=0;
for(int i=0;i<gg.length();i++){
if(gg.charAt(i)=='h'||gg.charAt(i)=='H'){if(chet==0){perv=i;}
                                         else{posl=i;}
                                         chet++;}}
System.out.println(gg.substring(0,posl)+gg.substring(perv+1));        
                                         
        // put your code here
    }
}


# C 
#include <stdio.h>
#include <wctype.h>

int main() {
    char str[1024] = "\0", sym = 'h';
    fgets(str, 1024, stdin);

    int i = 0, first = -1, last = -1;
    while(str[i]) {
        if (towlower(str[i]) == sym) {
            if (first == -1) first = i;
            last = i;
        }
        i++;
    }

    for (int i = 0; i <= first; i++) printf("%c", str[i]);

    for (int i = first + 1; i < last; i++) printf("%c", str[i]);
    for (int i = first + 1; i < last; i++) printf("%c", str[i]);
    
    for (int i = last; str[i]; i++) printf("%c", str[i]);
    
    return 0;
}


# C++
#include <iostream>

int main() {
    std::string s, res = "";
    std::getline(std::cin, s);
    
    int n1 = -1, n2 = -1;
    for (int i = 0; i < s.size(); i++) {
        if (s[i] == 'h' || s[i] == 'H') {
            if (n1 == -1) n1 = i;
            n2 = i;
        }
    }
    
    for (int i = 0; i < n2; i++) res += s[i];
    for (int i = n1 + 1; i <= n2; i++) res += s[i];
    for (int i = n2 + 1; i < s.size(); i++) res += s[i];
    std::cout << res << "\n";
    return 0;
}



