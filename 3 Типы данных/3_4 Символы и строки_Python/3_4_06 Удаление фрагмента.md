Удаление фрагмента

Дана строка, в которой буква h встречается минимум два раза (это может быть как маленькая буква h, так и заглавная H). Удалите из этой строки первое и последнее вхождение буквы h, а также все символы, находящиеся между ними.

Входные данные
Вводится строка.

Выходные данные
Выведите ответ на задачу.

Sample Input:
In the hole in the ground there lived a hobbit

Sample Output:
In tobbit

Напишите программу. Тестируется через stdin → stdout
Верно решили 366 учащихся
Из всех попыток 37% верных


# ruby
s = gets
#s = 'In tHe hole in the ground there lived a hobbit'
#s = 'In the hole in the ground there lived a h'
s_small = s.downcase
c = 'h'
s1 = s_small.index(c)
s2 = s_small.reverse.index(c)
s2 = s_small.size - 1 - s2
#puts s1, s2
puts s[0...s1] + s[s2+1..]


# python
s = input().replace('H', 'h')
print(s.replace(s[s.find('h'):s.rfind('h') + 1], ''))


# JS
const txt = require('fs').readFileSync(0, 'utf8');
let tmp = txt.toLowerCase();

let first_h = tmp.indexOf('h');
let last_h = tmp.lastIndexOf('h');

console.log(txt.slice(0, first_h) + txt.slice(last_h+1));


# C#
using System;
//using System.Threading;

public class MainClass
{
    public static void Main()
    {
        string str=Console.ReadLine();
       var sc=StringComparison.CurrentCultureIgnoreCase;        
       Console.WriteLine(str[..str.IndexOf("h",sc)]+str[(str.LastIndexOf("h",sc)+1)..]);
    }
}


# java
import java.util.Scanner;
class Main {
    public static void main(String[] args) {
Scanner v = new Scanner(System.in);
String gg=v.nextLine();int perv=0,posl=0,chet=0;
for(int i=0;i<gg.length();i++){
if(gg.charAt(i)=='h'||gg.charAt(i)=='H'){if(chet==0){perv=i;}
                                         else{posl=i;}chet++;}}  
System.out.println(gg.substring(0,perv)+gg.substring(posl+1));        
    }
}


# C 
#include <stdio.h>
#include <wctype.h>
#include <string.h>

int main() {
    char str[1024] = "\0", sym = 'h';
    fgets(str, 1024, stdin);

    int i = 0, first = -1, last = -1;
    while(str[i]) {
        if (towlower(str[i]) == sym) {
            if (first == -1) first = i;
            last = i;
        }
        i++;
    }    
    
    for (int i = 0; i < first; i++) printf("%c", str[i]);
    for (int i = last+1; str[i]; i++) printf("%c", str[i]);
    
    return 0;
}


# C++
#include <iostream>

int main() {
    std::string s, res = "";
    std::getline(std::cin, s);
    
    int n1 = -1, n2 = -1;
    for (int i = 0; i < s.size(); i++) {
        if (s[i] == 'h' || s[i] == 'H') {
            if (n1 == -1) n1 = i;
            n2 = i;
        }
    }
    
    for (int i = 0; i < n1; i++) res += s[i];
    for (int i = n2 + 1; i < s.size(); i++) res += s[i];
    std::cout << res << "\n";
    return 0;
}


# rust
use std::io::{self, Write};

fn main() {
    // Read a line from standard input (equivalent to Python's input())
    let mut input = String::new();
    io::stdin()
        .read_line(&mut input)
        .expect("Failed to read line");

    // Remove the trailing newline that `read_line` keeps
    let input = input.trim_end_matches(&['\n', '\r'][..]);

    // Replace all occurrences of 'H' with 'h' (same as Python's .replace('H', 'h'))
    let replaced = input.replace('H', "h");

    // Find the first and last occurrence of 'h'
    let first_h = replaced.find('h');
    let last_h = replaced.rfind('h');

    // If there is at least one 'h', remove the substring from the first to the last inclusive
    let result = match (first_h, last_h) {
        (Some(start), Some(end)) => {
            // `end + 1` is safe because `end` is a byte index of a char and Rust strings are UTF‑8.
            let before = &replaced[..start];
            let after = &replaced[end + 1..];
            format!("{}{}", before, after)
        }
        // No 'h' found – keep the string as‑is (only the 'H' → 'h' conversion applied)
        _ => replaced,
    };

    // Print the final string (equivalent to Python's print())
    println!("{}", result);
}






