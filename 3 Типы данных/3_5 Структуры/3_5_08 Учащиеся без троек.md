Учащиеся без троек

Выведите фамилии и имена учащихся, не имеющих троек (а также двоек и колов).

Входные данные
Заданы сначала количество учащихся n, затем n строк, каждая из которых содержит фамилию, имя и три числа (оценки по трем предметам: математике, физике, информатике). Данные в строке разделены одним пробелом. Оценки принимают значение от 1 до 5.

Выходные данные
Необходимо вывести пары фамилия-имя по одной на строке, разделяя фамилию и имя одним пробелом. Выводить оценки не нужно. Порядок вывода должен быть таким же, как в исходных данных.

Sample Input 1:
3
Babat Anna 5 4 3
Belova Galina 4 3 5
Moroz Yaroslav 3 5 4

Sample Output 1:

Sample Input 2:
2
Krasnov Ivan 5 5 5
Shusha Andrey 1 2 4

Sample Output 2:
Krasnov Ivan

Напишите программу. Тестируется через stdin → stdout
Верно решил 261 учащийся
Из всех попыток 68% верных



# ruby
n = gets.to_i

lmax, l, x, y = 0, 0, 0, 0
a = []
1.upto(n) do |i|
    a = gets.split().map{|j| j.to_s }
    #print a
    if (a.include?('1') or a.include?('2') or a.include?('3')) == false    
        print "#{a[0].to_s} #{a[1].to_s}\n"
    end    
end
#print ap


# python
n = int(input())
arr = [input().split(' ', 2) for _ in range(n)]
res = filter(lambda w: not (set('123') & set(w[2])), arr)
print(*[f'{w[0]} {w[1]}' for w in res], sep='\n')


# JS
var stdin = process.openStdin();

stdin.addListener("data", function(d) {
    let str = d.toString().split('\n');
    
    for ( let i=1; i<str.length; i++) {
        let [surname, name, a, b, s ] = str[i].split(' ');
        if ( a!=='1' && a!=='2' && a!=='3' && b!=='1' && b!=='2' && b!=='3' && s!=='1' && s!=='2' && s!=='3') console.log(surname+' '+name);
    }
  });


# C#
using System;
using System.Linq;
using System.Collections.Generic;
using System.Globalization;

public class MainClass
{
    public static void Main()
    {
        int check=int.Parse(Console.ReadLine());
        Study[]study=new Study [check];
        for (int i=0;i<check;i++)
           {
                var input=Console.ReadLine().Split(' ').ToArray();
                study[i]=new Study(input[0],input[1],int.Parse(input[2]),int.Parse(input[3]),int.Parse(input[4]));   
           }
        for (int i = 0;i<check;i++)
        {
            if(study[i].mathematic>3 && study[i].fysics>3 && study[i].informatic>3)
        Console.WriteLine($"{study[i].firstName} {study[i].lastName}");
        }  
    }
}
struct Study
{
    public string firstName;
    public string lastName;
    public int mathematic;
    public int fysics;
    public int informatic;
    
    public Study(string firstName, string lastName, int mathematic, int fysics, int informatic)
    {
        this.firstName = firstName;
        this.lastName = lastName;
        this.mathematic = mathematic;
        this.fysics = fysics;
        this.informatic = informatic;
    }
    
}


# java
#include <inttypes.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

struct diary {
    char surname[32];
    char name[32];
    uint8_t math;
    uint8_t phys;
    uint8_t info;
};

void echo_double(double x, uint8_t afterdot);
size_t input(struct diary** array);
double get_m_math(struct diary** array, size_t n);
double get_m_phys(struct diary** array, size_t n);
double get_m_info(struct diary** array, size_t n);
void output_fullnames_with_45(struct diary** array, size_t n);

int main() {
    struct diary* students = NULL;
    size_t count = input(&students);
    if (count > 0) {
        output_fullnames_with_45(&students, count);
        free(students);
    }
    return 0;
}

void output_fullnames_with_45(struct diary** array, size_t n) {
    for (size_t i = 0; i < n; i++)
        if ((*array)[i].math > 3 && (*array)[i].phys > 3 && (*array)[i].info > 3)
            printf("%s %s\n", (*array)[i].surname, (*array)[i].name);
}

double get_m_info(struct diary** array, size_t n) {
    double m = 0.0;
    for (size_t i = 0; i < n; i++) m += (*array)[i].info;
    return m / n;
}

double get_m_phys(struct diary** array, size_t n) {
    double m = 0.0;
    for (size_t i = 0; i < n; i++) m += (*array)[i].phys;
    return m / n;
}

double get_m_math(struct diary** array, size_t n) {
    double m = 0.0;
    for (size_t i = 0; i < n; i++) m += (*array)[i].math;
    return m / n;
}

size_t input(struct diary** array) {
    size_t n = 0;
    scanf("%zu", &n);
    (*array) = malloc(n * sizeof(struct diary));
    if ((*array) == NULL) {
        n = 0;
    } else {
        for (size_t i = 0; i < n; i++) {
            scanf("%s", &((*array)[i].surname));
            scanf("%s", &((*array)[i].name));
            scanf("%"SCNu8, &((*array)[i].math));
            scanf("%"SCNu8, &((*array)[i].phys));
            scanf("%"SCNu8, &((*array)[i].info));
        }
    }
    return n;
}
   
// до 15и значащих цифр после запятой, не больше afterdot
void echo_double(double x, uint8_t afterdot) {
    size_t maxlen = 127;
    char numstr[maxlen];
    sprintf(numstr, "%.15lf", x);
    // удаление не значащих цифр после запятой (больше afterdot)
    size_t numlen = strlen(numstr);
    uint8_t delta = 15 - afterdot;
    for (size_t i = 0; i < delta; i++)
        numstr[numlen - 1 - i] = '\0';
    // удаление не значащих нулей после запятой
    numlen = strlen(numstr);
    size_t idot = strcspn(numstr, ".");
    for (size_t i = numlen - 1; i > idot + 1; i--)
        if (numstr[i] == '0') numstr[i] = '\0';
        else break;
    printf("%s", numstr);
}


# C
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#define strsize(args...) (snprintf(NULL, 0, args) + sizeof('\0'))
#define NUM_OF_SUBJECTS 3

typedef struct Student
{
    char *name;
    char *surname;
    int rating[NUM_OF_SUBJECTS];
} Student, *PStudent;


void display_students(PStudent students, size_t students_count)
{
    size_t count = 0;
    while (count < students_count)
    {
        /*printf("Name: %s Surname: %s ", students[count].name, students[count].surname);
        printf("Rating: ");
        for (int i = 0; i < NUM_OF_SUBJECTS; i++)
            printf("%d ", students[count].rating[i]);
        printf("\n");*/
        printf("%s %s\n", students[count].name, students[count].surname);
        count++;
    }
}


PStudent fill_info_about_students(size_t students_count)
{
    PStudent    students;
    size_t      count = 0;
    char        buf_name[BUFSIZ];
    char        buf_surname[BUFSIZ];

    students = (PStudent)malloc(sizeof(Student) * students_count);
    while (count != students_count)
    {
        scanf("%s %s", buf_name, buf_surname);
        for (int i = 0; i < NUM_OF_SUBJECTS; i++)
            scanf("%d", &students[count].rating[i]);

        students[count].name = (char*)malloc(strsize("%s", buf_name));
        students[count].surname = (char*)malloc(strsize("%s", buf_surname));
        strcpy(students[count].name, buf_name);
        strcpy(students[count].surname, buf_surname);

        count++;
    }
    return students;
}


void free_students(PStudent students, size_t students_count)
{
    size_t count = 0;

    while (count < students_count)
    {
        free(students[count].name);
        free(students[count].surname);
        count++;
    }
    free(students);
}


PStudent find_students(PStudent students, size_t students_count, size_t *found_count)
{
    PStudent found_students = NULL;
    size_t   count = 0;
    int      is_good = 1; /* Флаг для проверки оценок */
    *found_count = 0;

    while (count < students_count)
    {
        for (int i = 0; i < NUM_OF_SUBJECTS; i++)
            if (students[count].rating[i] <= 3)
            {
                is_good = 0;
                break;
            }

        if (is_good)
        {
            found_students = (PStudent)realloc(found_students, ++(*found_count) * sizeof(Student));
            found_students[*found_count - 1].name = (char*)malloc(strsize("%s", students[count].name));
            found_students[*found_count - 1].surname = (char*)malloc(strsize("%s", students[count].surname));
            strcpy(found_students[*found_count - 1].name, students[count].name);
            strcpy(found_students[*found_count - 1].surname, students[count].surname);

            for (int i = 0; i < NUM_OF_SUBJECTS; i++)
                found_students[*found_count - 1].rating[i] = students[count].rating[i];
        }
        is_good = 1;
        count++;
    }
    return found_students;
}


int main()
{
    PStudent    students = NULL;
    size_t      students_count;
    PStudent    sought_students = NULL;
    size_t      sought_count;

    scanf("%zu", &students_count);
    students = fill_info_about_students(students_count);
    sought_students = find_students(students, students_count, &sought_count);
    display_students(sought_students, sought_count);
    free_students(sought_students, sought_count);
    free_students(students, students_count);
    return 0;
}


# C++
#include <iostream>

int main() {
    int n;
    std::cin >> n;
    while (n--) {
        std::string name, surname;
        int a1, a2, a3;
        std::cin >> name >> surname >> a1 >> a2 >> a3;
        if (a1 > 3 && a2 > 3 && a3 > 3) std::cout << name << " " << surname << "\n"; 
    }
    return 0;
}
