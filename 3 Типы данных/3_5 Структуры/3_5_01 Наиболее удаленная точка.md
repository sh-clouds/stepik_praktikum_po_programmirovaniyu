Наиболее удаленная точка

Выведите координаты наиболее удаленной от начала координат точки.

Входные данные
Программа получает на вход набор точек на плоскости. Сначала задано количество точек n, затем идет последовательность из n строк, каждая из которых содержит два числа: координаты точки. Величина n не превосходит 100, все исходные координаты – целые числа, не превосходящие 10^3 по абсолютной величине.

Выходные данные
Выведите  координаты точки, наиболее удаленной от начала координат.

Sample Input:
2
1 2
2 3

Sample Output:
2 3

Напишите программу. Тестируется через stdin → stdout
Верно решили 334 учащихся
Из всех попыток 43% верных



# ruby
n = gets.to_i
lmax, l, x, y = 0, 0, 0, 0

1.upto(n) do |i|
    a = gets.split()
    l = (a[0].to_i**2 + a[1].to_i**2).to_f**0.5
    if lmax < l 
        lmax = l
        x = a[0].to_i
        y = a[1].to_i
    end
end

print "#{x} #{y}"


# python
d = {}
for _ in range(int(input())):
    a, b = map(int, input().split())
    c = (a ** 2 + b ** 2) ** 0.5
    d[c] = a, b
print(*d[max(d)])


# JS
var stdin = process.openStdin();

stdin.addListener("data", function(d) {
    let data = d.toString().split('\n');
    let count = 0;
    let result = '';
    
    for (let i=1; i<data.length; i++) {
        let [x, y] = data[i].split(' ');
        let length = (x*x + y*y)**0.5;
        if ( count < length) { 
            count = length;
            result = x+' '+y;
        }
    }    
    console.log(result);
  });

  
# C#
using System;
using System.Linq;
using System.Collections.Generic;

namespace hh
{
public class MainClass
{
    public static void Main()
    {
   int check=int.Parse(Console.ReadLine());
   Point[]points=new Point [check];
   double maxx=0, maxy=0;
   int xx=0, yy=0;
   for (int i=0;i<check;i++)
   {
    var coordinates=Console.ReadLine().Split(' ').Select(x=>int.Parse(x)).ToArray();
    points[i]=new Point(coordinates[0], coordinates[1]);
   }
   for (int i=0;i<check;i++)
   {
     maxx=Math.Sqrt(Math.Pow(points[i].x,2)+Math.Pow(points[i].y,2));
      if (maxx>maxy)
      {
        maxy=maxx;
      xx=points[i].x;
      yy=points[i].y;
      }   
   }
   Console.WriteLine($"{xx} {yy}");
    }
}
struct Point
    {
        public int x;
        public int y;
        
        public Point(int x, int y)
        {
            this.x=x;
            this.y=y;
        }
     }
  }


# java
import java.util.*;

public class Main {
    public static void main(String[] args) {
        Scanner input = new Scanner(System.in);

        int number = Integer.parseInt(input.nextLine());
        List<Point> points = new ArrayList<>();

        for (int a = 0; a < number; a++) {
            points.add(new Point(input.nextLine().split("\\s+")));
        }

        Collections.sort(points, (a, b) -> Double.compare(b.distance(), a.distance()));
        System.out.println(points.get(0));
    }
}

class Point {
    private int x;
    private int y;

    Point(String[] line) {
        this.x = Integer.parseInt(line[0]);
        this.y = Integer.parseInt(line[1]);
    }

    double distance() {
        return Math.sqrt(Math.pow(x, 2) + Math.pow(y, 2));
    }

    @Override
    public String toString() {
        return String.format("%d %d", x, y);
    }
}


# C
#include <stdio.h>
#include <math.h>

struct point {
    int x;
    int y;
};

int main() {
    struct point dots[100];
    int n = 0;
    scanf("%d", &n);
    for (int i = 0; i < n; i++) scanf("%d %d", &dots[i].x, &dots[i].y);

    int imax = 0;
    double smax = sqrt(dots[imax].x * dots[imax].x + dots[imax].y * dots[imax].y);
    for (int i = 1; i < n; i++) {
        double si = sqrt(dots[i].x * dots[i].x + dots[i].y * dots[i].y);
        if (si > smax) {
            imax = i;
            smax = si;
        }
    }
    printf("%d %d", dots[imax].x, dots[imax].y);

    return 0;
}


# C++
#include <iostream>
#include <cmath>

struct coord {
    int vert;
    int hor;
};

int main() {
    // put your code here
    int n = 0;
    double res = -1;
    std::cin >> n;
    
    int maxX = 0, maxY = 0;
    
    for(size_t i = 0; i < n; ++i){
        coord point;
        std::cin >> point.hor >> point.vert;
        double temp = sqrt(point.vert * point.vert + point.hor * point.hor);
        if(temp >= res) {
            res = temp;
            maxY = point.vert;
            maxX = point.hor;
        }
    }
    
    std::cout << maxX << " " << maxY << std::endl;
    return 0;
}



