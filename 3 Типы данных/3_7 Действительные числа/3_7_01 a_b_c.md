a + b = c

Даны три действительных числа: a, b, c. Проверьте, выполняется ли равенство a + b = c . Если равенство выполняется, выведите YES, если не выполняется, выведите NO.

Входные данные
Числа a, b, c –  действительные, положительные, не превосходят 10 и заданы не более, чем с 7 знаками после точки.

Выходные данные
Выведите результат сравнения.

Sample Input 1:
2
3
7

Sample Output 1:
NO

Sample Input 2:
0.1
0.2
0.3

Sample Output 2:
YES

Напишите программу. Тестируется через stdin → stdout
Верно решили 427 учащихся
Из всех попыток 42% верных


# ruby
a,b,c = gets.to_f, gets.to_f, gets.to_f
#a,b,c = 0.1, 0.2, 0.3
#puts a+b,c
if (a+b).round(7)==c.round(7)
    puts "YES"
else
    puts "NO"    
end  


# python
from decimal import Decimal
print('YES' if Decimal(input()) + Decimal(input()) == Decimal(input()) else 'NO')


# JS
process.stdin.on('readable', () => {
    let data = process.stdin.read();
    if (data) {
        [a, b, c] = data.toString().split('\n');
        console.log(Math.abs(+a + +b - +c) < Number.EPSILON ? 'YES' : 'NO');
    }
});


# C#
using System;

public class MainClass
{
    public static void Main()
    {
        float a = float.Parse(Console.ReadLine());
        float b = float.Parse(Console.ReadLine());
        float c = float.Parse(Console.ReadLine());
        string ans = a+b==c ? "YES":"NO";
        Console.WriteLine(ans);
    }
}


# java
import java.util.Scanner;
class Main {
    public static void main(String[] args) {
Scanner v=new Scanner(System.in);
double a=v.nextDouble(),b=v.nextDouble(),c=v.nextDouble(),c1;
c1=Math.round((a+b)*Math.pow(10,7))/Math.pow(10,7);
System.out.println(c1==c?"YES":"NO");        // put your code here
    }
}


# C
#include <stdio.h>
#include <math.h>

int main() {
    double a, b, c;
    scanf("%lf %lf %lf", &a, &b, &c);
    a = floor(a * pow(10, 7));
    b = floor(b * pow(10, 7));
    c = floor(c * pow(10, 7));
    printf("%s", (a + b == c) ? "YES" : "NO");
    return 0;
}


# C++
#include <iostream>
#include <cmath>
using namespace std;

int main() {
    double a, b, c; cin >> a >> b >> c;
    cout << (abs(a + b - c) < 0.0000001 ? "YES" : "NO");
    return 0;
}


