Конвертирование

Дана строка S, в которой выделили подстроку, состоящую из символов с i-го по j-й включительно (символы строки S нумеруются с единицы) и поменяли местами i-й символ с j-м, (i+1)-й с (j-1)-м и так далее (конвертировали подстроку). Выведите строку S после внесенных изменений.

Входные данные
В первой строке входного файла содержится строка S, длиной не более 1000 символов, во второй – числа i и  j (i ≤  j).

Выходные данные
В выходной файл выведите ответ на задачу.

Sample Input:
vjhoamkts
7 8

Sample Output:
vjhoamtks

Напишите программу. Тестируется через stdin → stdout
Верно решили 387 учащихся
Из всех попыток 44% верных


# ruby
s, a = gets.chomp, gets.chomp.split(' ').map { |j| j.to_i }
s2 = s[...a[0]-1] + s[a[0]-1..a[1]-1].reverse + s[a[1]...]
#vjhoamtks
#s3 = s[...a[0]-1]  #vjhoam
#s4 = s[a[0]-1..a[1]-1].reverse #tk
#s5 = s[a[1]...]
puts s2


# python
s = list(input())
i, j = map(int, input().split())
i -= 1
s[i:j] = s[i:j][::-1]
print(''.join(s))


# JS
process.stdin.on('readable', () => {
    let data = process.stdin.read();
    if (data) {
        const [[...chrs], idxs] = data.toString().split('\n');
        const [i, j] = idxs.split(' ');
        chrs.splice( i - 1, j - i + 1, ...chrs.slice(i - 1, j).reverse() );
        console.log(chrs.join(''));
    }
});


# C#
using System;
using System.Linq;

public class MainClass
{
    public static void Main()
    {
     char[]chars=Console.ReadLine().ToCharArray();
var num=Console.ReadLine().Split(' ').ToArray();
     int i=(int.Parse(num[0]))-1;
     int j=(int.Parse(num[1]))-1;
    
     while(i<j)
     {
         char temp = chars[i];
         chars[i]=chars[j];
         chars[j]=temp;
         i++; j--;
     }
     Console.WriteLine(new string(chars));
    }
}


# java
import java.util.Scanner;
class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        String [] arr = sc.next().split("");
        int n1 = sc.nextInt()-1;
        int n2 = sc.nextInt()-1;
        sc.close();
        
        for (int i = n1; n1 < n2; n1++, n2--) {
            String temp = arr[n1];
            arr[n1] = arr[n2];
            arr[n2] = temp;
        }
        
        System.out.print(String.join("",arr));
    }
}


# C
#include <stdio.h>
#include <stdlib.h>
#define BUFF_SIZE 10000

int main() {
  char str[BUFF_SIZE];
  size_t left_bound, right_bound;
  if (scanf("%s %zu %zu", str, &left_bound, &right_bound) != 3) return EXIT_FAILURE;
  left_bound--, right_bound--;
  while (left_bound < right_bound) {
    size_t tmp = str[left_bound];
    str[left_bound] = str[right_bound];
    str[right_bound] = tmp;
    left_bound++, right_bound--;
  }
  printf("%s\n", str);
  return EXIT_SUCCESS;
}


# C++
#include <iostream>

int main() {
    std::string s;
    int i, j;
    std::cin >> s >> i >> j;
    for (i -= 1, j -= 1; i < j; i++, j--) {
        char t = s[i];
        s[i] = s[j];
        s[j] = t;
    }
    std::cout << s << "\n";
    return 0;
}



