Разные

Дано N чисел, требуется выяснить, сколько среди них различных.

Входные данные
В первой строке дано число N – количество чисел. (1 <= N <= 100000) Во второй строке даны через пробел N чисел, каждое не превышает 2*109 по модулю.

Выходные данные
Выведите число, равное количеству различных чисел среди данных.

Sample Input:
5
1 0 1 2 0

Sample Output:
3

Напишите программу. Тестируется через stdin → stdout
Верно решили 508 учащихся
Из всех попыток 74% верных


# ruby
n = gets.to_i 
a = gets.split().map{|j| j.to_i }
a = a | a
puts a.size


# python
input(); print(len(set(input().split())))


# JS
let [arr] = require('fs').readFileSync(0, 'utf8').split('\n').slice(1);
const set_arr = new Set(arr.split(' '));
console.log(set_arr.size); 


# C#
using System;
using System.Collections.Generic;
using System.Linq;

public class MainClass
{
    public static void Main()
    {
        string n=Console.ReadLine();
        var result=Console.ReadLine().Split().Distinct();
    Console.WriteLine(result.Count());
    }    
}


# java
import java.util.Scanner;
import java.util.Arrays;
class Main {
    public static void main(String[] args) {
Scanner v = new Scanner (System.in);        // put your code here
int n=v.nextInt(),min,chet=1;
int[] vv=new int[n];
for(int i=0;i<n;i++){
vv[i]=v.nextInt();}
Arrays.sort(vv);
min=vv[0];
for(int i=1;i<n;i++){
if(min<vv[i]){min=vv[i];chet++;}}    
System.out.println(chet);    
        
    }
}


# C
#include <inttypes.h>
#include <stdio.h>
#include <stdlib.h>

size_t input(int32_t** array);
size_t count_different(int32_t** array, size_t n);

int main() {
    int32_t* many;
    size_t count = input(&many);
    if (count > 0) {
        printf("%zu", count_different(&many, count));
        free(many);
    }
    return 0;
}

size_t count_different(int32_t** array, size_t n) {
    size_t m = 1;
    for (size_t i = 1; i < n; i++)
        for (size_t j = 0; j < i; j++) {
            if ((*array)[j] == (*array)[i]) break;
            if (j == i - 1) m++;
        }
    return m;
}

size_t input(int32_t** array) {
    size_t n = 0;
    scanf("%zu", &n);
    if (n > 0) {
        (*array) = malloc(n * sizeof(int32_t));
        if ((*array) == NULL) n = 0;
        for (size_t i = 0; i < n; i++) scanf("%"SCNd32, &((*array)[i]));
    }
    return n;
}


# C++
#include <iostream>
#include <set>

int main() {
    // put your code here
    std::set <int> number;
    int n;
    std::cin >> n;
    
    for(int i = 0; i < n; ++i) {
        int temp;
        std::cin >> temp;
        number.insert(temp);
    }
    
    std::cout << number.size() << std::endl;
    return 0;
}



